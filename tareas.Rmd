---
title: "tareas y ejercicios"
author: "Nestor Flores Castañeda"
date: "2023-05-25"
output:
  html_document: default
  word_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# tarea, sesion 1.


 GENERAR UNA MATRIZ ALEATORIA DE 7X7, ETIQUETAR LAS COLUMNAS, LOS RENGLONES

Matriz de 7 dias y 7 meses, con numeros aleatorios
```{r }
n <- 7; m <- 7
B <- matrix(0,n,m)
for(i in 1:n){
  for(j in 1:m){
    B[i,j] <- ceiling(runif(1,0,30))}}
colnames(B)<-c('Lunes','Martes','Miercoles','Jueves','Viernes','Sabado','Domingo')
rownames(B)<- c('Enero','Febrero', 'Marzo','Abril','Mayo','Junio','Julio')
print(B)
```

    PRIMEROS 3 RENGLONES Y LAS PRIMERAS 4 COLUMNAS

```{r }
B[1:3,1:4]

```
     Mostrara los ULTIMOS 4 RENGLONES Y LAS ULTIMAS 4 COLUMNA
```{r }
B[4:7,4:7]
```
     Mostrara las colunas impares
```{r }

indices <- c(1,3,5,7)
B[,indices]

```
     Mostrara los renglones pares
```{r }

indices <- c(2,4,6)
B[indices,]

```
    Mostrara renglones y columnas impares



```{r }

indices <- c(1,3,5,7);indi<- c(1,3,5,7)
B[indices,indi]

```

    Mostrara renglones y columnas pares


```{r }
indices <- c(2,4,6);indi<- c(2,4,6)
B[indices,indi]
```

      Mostrara renglones pares y columnas impares


```{r }
indices<- c(2,4,6); indi <- c(1,3,5,7)
B[indices,indi]
```

Mostrara renglones impares y columnas pares 


```{r }
indices<- c(1,3,5,7) ; indi<- c(2,4,6)
B[indices,indi]

```

 4 matrices entre -50,50, 7x7 

floor,ceiling,trunc,round


```{r }
n <- 7; m <- 7
M1 <- matrix(0,n,m)
M2 <- matrix(0,n,m)
M3 <- matrix(0,n,m)
M4 <- matrix(0,n,m)
for(i in 1:n){
  for(j in 1:m){
    entrada <- runif(1,-50,50)
    M1[i,j] <- floor(entrada)
    M2[i,j] <- ceiling(entrada)
    M3[i,j] <- trunc(entrada)
    M4[i,j] <- round(entrada)
  }
}
M1
M2
M3
M4
```
Operacion de Matrices
```{r }
M1-M2
M3+M4
M4/M2
M2*M3

```

Comparacion de matrices


```{r }

D <-  matrix(0,n,m)
L <-  matrix(0,n,m)
U <-  matrix(0,n,m)
for(i in 1:n){
  for(j in 1:m){
    if(i==j){D[i,j]<- ceiling(runif(1,-50,50))}
    if(j<=i){L[i,j]<- ceiling(runif(1,-50,50))}
    if(j>=i){U[i,j]<- ceiling(runif(1,-50,50))}
  }
}

L <-  matrix(0,n,m)
for(i in 1:n){
  for(j in 1:i){
    L[i,j]<- ceiling(runif(1,-50,50))
  }
}
print(L)

for(i in 1:n){
  for(j in i:m){
    U[i,j]<- ceiling(runif(1,-50,50))
  }
}
print(U)

```
# Sección 1.1: Variables Aleatorias Discretas
## Subsección 1.1.1: Distribución Uniforme
1. Un juego de dados justo se lanza tres veces. ¿Cuál es la probabilidad de obtener una suma de 10?


```{r }
probabilidad_suma_10 <- function() {
 resultados_favorables <- 0
 for (dado1 in 1:6) {
 for (dado2 in 1:6) {
 for (dado3 in 1:6) {
 
 if ((dado1 + dado2 + dado3) == 10) {
 resultados_favorables <- resultados_favorables + 1}}}}
 probabilidad <- resultados_favorables / (6^3)
 cat("La probabilidad de obtener una suma de 10 al lanzar tres dados es:",
 probabilidad * 100, "%")
}
probabilidad_suma_10()

```

2. Una urna contiene 10 bolas numeradas del 1 al 10. Si se elige una bola al azar, ¿cuál es la probabilidad de que
sea un número par?

```{r }
num_bolas_pares <- 5
num_total_bolas <- 10
probabilidad_par <- num_bolas_pares / num_total_bolas
cat("La probabilidad de elegir una bola par al azar es:",
 probabilidad_par * 100, "%")

```

3. Un fabricante de piezas de repuesto sabe que el 5 % de las piezas que produce son defectuosas. Si se eligen
aleatoriamente 5 piezas, ¿cuál es la probabilidad de que al menos una de ellas sea defectuosa?

```{r }
probabilidad_defectuosa <- 1 - (0.95)^5
cat("La probabilidad de que al menos una de las 5 piezas sea defectuosa es:",probabilidad_defectuosa * 100, "%")

```

4. Un fabricante de cartas de póker quiere asegurarse de que todas las cartas tengan la misma probabilidad de
ser seleccionadas al azar. ¿Cuántas cartas debe incluir en su mazo?


```{r }
num_cartas <- 52
cat("El mazo debe incluir", num_cartas, "cartas.")
```

5. Un estudiante elige al azar una página de un libro y cuenta el número de palabras que contiene. Si el libro
tiene 200 páginas y un promedio de 1000 palabras por página, ¿cuál es la probabilidad de que el número de
palabras en una página elegida al azar esté entre 900 y 1100?


```{r }
limite_inferior <- 900
limite_superior <- 1100
media <- 1000
desviacion_estandar <- 100
z_inferior <- (limite_inferior - media) / desviacion_estandar
z_superior <- (limite_superior - media) / desviacion_estandar
probabilidad <- pnorm(z_superior) - pnorm(z_inferior)
cat("La probabilidad de que el numero de palabras en una pagina 
 elegida al azar este entre 900 y 1100 es:", probabilidad * 100, "%")

```
6. Un vendedor de seguros tiene 10 pólizas que puede vender. Si se eligen al azar 3 pólizas, ¿cuál es la probabilidad
de que al menos una de ellas sea vendida?



```{r }
num_casos_fav <- choose(10, 3)
num_total_casos <- choose(10, 3)
probabilidad_ninguna_vendida <- num_casos_fav / num_total_casos
probabilidad_vendida <- 1 - probabilidad_ninguna_vendida
cat("La probabilidad de que al menos una de las tres polizas sea vendida es:",
 probabilidad_vendida * 100, "%")

```

7. Un fabricante de dulces quiere que cada bolsa contenga una cantidad igual de caramelos de cada sabor. Si hay
4 sabores de caramelos y cada bolsa contiene 20 caramelos, ¿cuál es la probabilidad de que haya exactamente 5
caramelos de cada sabor en una bolsa seleccionada al azar?


```{r }
num_casos_fav <- choose(20, 5)
num_total_casos <- choose(20, 5) * choose(15, 5) * choose(10, 5) * choose(5, 5)
probabilidad <- num_casos_fav / num_total_casos
cat("La probabilidad de que haya exactamente 5 caramelos de cada sabor 
 en una bolsa seleccionada al azar es:", probabilidad * 100, "%")

```

8. Una persona lanza una moneda 5 veces. ¿Cuál es la probabilidad de que obtenga exactamente 2 caras?

```{r }
num_casos_fav <- choose(5, 2)
num_total_casos <- 2^5
probabilidad <- num_casos_fav / num_total_casos
cat("La probabilidad de obtener exactamente 2 caras al lanzar 
 una moneda 5 veces es:", probabilidad * 100, "%")

```

9. Un jugador de fútbol tiene una tasa de éxito del 80 % en tiros libres. Si tira 5 tiros libres, ¿cuál es la probabilidad
de que haga exactamente 4 goles?


```{r }
num_casos_fav <- choose(5, 4)
probabilidad_gol <- 0.8
probabilidad_no_gol <- 1 - probabilidad_gol
num_total_casos <- probabilidad_gol^4 * probabilidad_no_gol
probabilidad <- num_casos_fav * num_total_casos
cat("La probabilidad de que el jugador haga exactamente 4 goles 
 en 5 tiros libres es:", probabilidad * 100, "%")

```

10. Se lanza un dado justo hasta obtener un número par. ¿Cuál es la probabilidad de que se necesiten al menos 3
lanzamientos?

```{r }
probabilidad_max_2_lanzamientos <- 1/4
probabilidad_al_menos_3_lanzamientos <- 1 - probabilidad_max_2_lanzamientos
cat("La probabilidad de que se necesiten al menos 3 lanzamientos es:",
 probabilidad_al_menos_3_lanzamientos * 100, "%")

```
# 1.1.2. Subsección 1.1.2: Distribución Bernoulii

1. Un jugador de baloncesto tiene una tasa de acierto del 70 % en tiros libres. ¿Cuál es la probabilidad de que
haga el primer tiro libre?

```{r }
probabilidad_exito <- 0.70
probabilidad_primer_tiro_libre <- probabilidad_exito
cat("La probabilidad de que el jugador haga el primer tiro libre es:",
 probabilidad_primer_tiro_libre * 100, "%")

```
2. Un anuncio en línea tiene una tasa de clics del 2 %. Si se muestra el anuncio a 1000 personas, ¿cuál es la
probabilidad de que exactamente 20 personas hagan clic en él?


```{r }
n <- 1000
k <- 20
p <- 0.02
probabilidad_20_clics <- choose(n, k) * p^k * (1 - p)^(n - k)
cat("La probabilidad de que exactamente 20 personas hagan clic 
 en el anuncio es:", probabilidad_20_clics * 100, "%")

```
3. Un fabricante de bombillas sabe que el 3 % de las bombillas que produce son defectuosas. Si se elige una
bombilla al azar, ¿cuál es la probabilidad de que sea defectuosa?


```{r }
probabilidad_defectuosa <- 0.03
cat("La probabilidad de que una bombilla seleccionada al azar 
 sea defectuosa es:", probabilidad_defectuosa * 100, "%")

```
4. Un jugador de fútbol americano tiene una tasa de éxito del 90 % en tiros de campo. Si intenta un tiro de
campo, ¿cuál es la probabilidad de que tenga éxito?


```{r }
probabilidad_exito <- 0.90
cat("La probabilidad de que el jugador tenga exito en un intento 
 de tiro de campo es:", probabilidad_exito * 100, "%")
```

5. La probabilidad de que un jugador de baloncesto haga un tiro libre es de 0.85. Si un jugador tira un solo tiro
libre, ¿cuál es la probabilidad de que lo haga?

```{r }
probabilidad_exito <- 0.85
cat("La probabilidad de que el jugador haga un tiro libre es:", 
 probabilidad_exito * 100, "%")


```
6. La probabilidad de que una persona enferma de gripe tenga fiebre es de 0.9. Si se selecciona aleatoriamente a
una persona enferma de gripe, ¿cuál es la probabilidad de que tenga fiebre?



```{r }
probabilidad_exito <- 0.9
cat("La probabilidad de que una persona enferma de gripe tenga 
 fiebre es:", probabilidad_exito * 100, "%")

```

7. La probabilidad de que un estudiante apruebe un examen es de 0.6. Si se selecciona aleatoriamente a un
estudiante, ¿cuál es la probabilidad de que apruebe su examen?

```{r }
probabilidad_exito <- 0.6
cat("La probabilidad de que un estudiante apruebe su examen es:",
 probabilidad_exito * 100, "%")

```

8. La probabilidad de que una persona que juega a la ruleta gane en una ronda es de 0.2. Si una persona juega
una sola ronda, ¿cuál es la probabilidad de que gane?


```{r }
probabilidad_exito <- 0.2
cat("La probabilidad de que una persona gane en una ronda de 
 ruleta es:", probabilidad_exito * 100, "%")

```

9. La probabilidad de que una persona contratada por una empresa sea un buen empleado es de 0.7. Si una
empresa contrata a una sola persona, ¿cuál es la probabilidad de que sea un buen empleado?


```{r }
probabilidad_exito <- 0.7
cat("La probabilidad de que una persona contratada sea un buen 
 empleado es:", probabilidad_exito * 100, "%")

```
10. La probabilidad de que una persona en una ciudad determinada tenga seguro médico es de 0.4. Si se selecciona
aleatoriamente a una persona en la ciudad, ¿cuál es la probabilidad de que tenga seguro médico?


```{r }
probabilidad_exito <- 0.4
cat("La probabilidad de que una persona tenga seguro medico es:",
 probabilidad_exito * 100, "%")

```
11. La probabilidad de que una persona llegue a tiempo a una cita es de 0.8. Si una persona tiene una sola cita,
¿cuál es la probabilidad de que llegue a tiempo?


```{r }
probabilidad_exito <- 0.8
cat("La probabilidad de que una persona llegue a tiempo a 
 su cita es:", probabilidad_exito * 100, "%")

```
12. La probabilidad de que un estudiante se enferme durante un semestre es de 0.3. Si se selecciona aleatoriamente
a un estudiante, ¿cuál es la probabilidad de que se enferme durante el semestre?


```{r }
probabilidad_exito <- 0.3
cat("La probabilidad de que un estudiante se enferme durante el 
 semestre es:", probabilidad_exito * 100, "%")

```
13. La probabilidad de que un equipo de fútbol gane un partido es de 0.6. Si un equipo juega un solo partido,
¿cuál es la probabilidad de que gane?


```{r }
probabilidad_exito <- 0.6
cat("La probabilidad de que un equipo de futbol gane un partido es:",
 probabilidad_exito * 100, "%")

```
14. La probabilidad de que una persona responda correctamente una pregunta en un examen de opción múltiple es
de 0.25. Si una persona responde una sola pregunta, ¿cuál es la probabilidad de que responda correctamente?


```{r }
probabilidad_exito <- 0.25
cat("La probabilidad de que una persona responda correctamente 
 una pregunta es:", probabilidad_exito * 100, "%")

```
# 1.1.3. Subsección 1.1.3: Distribución Binomial

1. Una fábrica de chocolate sabe que el 10 % de sus barras de chocolate tienen algún defecto. Si se seleccionan al
azar 5 barras, ¿cuál es la probabilidad de que al menos una tenga defectos?
```{r }
probabilidad_exito <- 0.1
n <- 5
probabilidad_complementaria <- 1 - dbinom(0, n, probabilidad_exito)
cat("La probabilidad de que al menos una barra tenga defectos es:"
 , probabilidad_complementaria * 100, "%")

```

2. Un agente de bienes raíces sabe que la probabilidad de que un comprador cierre un trato es del 40 %. Si el
agente tiene 8 posibles compradores interesados, ¿cuál es la probabilidad de que exactamente 3 cierren un
trato?


```{r }
probabilidad_exito <- 0.4
n <- 8
k <- 3
probabilidad_exactamente_tres <- dbinom(k, n, probabilidad_exito)
cat("La probabilidad de que exactamente 3 compradores cierren
 un trato es:", probabilidad_exactamente_tres * 100, "%")

```

3. Un equipo de béisbol tiene una tasa de éxito del 70 % en bateo. Si el equipo batea 25 veces, ¿cuál es la
probabilidad de que al menos 18 de los bateos sean exitosos?


```{r }
probabilidad_exito <- 0.7
n <- 25
k <- 18
probabilidad_al_menos_dieciocho <- sum(dbinom(k:n, n, probabilidad_exito))
cat("La probabilidad de que al menos 18 bateos sean exitosos es:",
 probabilidad_al_menos_dieciocho * 100, "%")

```
4. Una empresa de marketing sabe que la tasa de respuesta a un correo electrónico es del 20 %. Si envían 100
correos electrónicos, ¿cuál es la probabilidad de que menos de 10 personas respondan?


```{r }
probabilidad_exito <- 0.2
n <- 100
k <- 9
probabilidad_menos_de_diez <- sum(dbinom(0:k, n, probabilidad_exito))
cat("La probabilidad de que menos de 10 personas respondan es:",
 probabilidad_menos_de_diez * 100, "%")

```
5. Un investigador sabe que la probabilidad de que un ratón de laboratorio tenga una mutación específica es del
5 %. Si el investigador utiliza 30 ratones, ¿cuál es la probabilidad de que al menos uno tenga la mutación?



```{r }
probabilidad_exito <- 0.05
n <- 30
probabilidad_al_menos_uno <- 1 - dbinom(0, n, probabilidad_exito)
cat("La probabilidad de que al menos uno de los ratones tenga 
 la mutacion es:", probabilidad_al_menos_uno * 100, "%")

```
6. Una compañía de seguros sabe que la probabilidad de que un cliente haga un reclamo es del 10 %. Si la
compañía tiene 500 clientes, ¿cuál es la probabilidad de que al menos 50 hagan un reclamo?


```{r }
probabilidad_exito <- 0.1
n <- 500
k <- 50
probabilidad_al_menos_cincuenta <- sum(dbinom(k:n, n,probabilidad_exito))
cat("La probabilidad de que al menos 50 clientes hagan un reclamo es:",
 probabilidad_al_menos_cincuenta * 100, "%")

```
7. Un centro de llamadas sabe que la probabilidad de que un agente cierre una venta es del 25 %. Si el centro de
llamadas tiene 20 agentes, ¿cuál es la probabilidad de que al menos 5 cierren ventas en un día?



```{r }
probabilidad_exito <- 0.25
n <- 20
k <- 5
probabilidad_al_menos_cinco <- sum(dbinom(k:n, n, probabilidad_exito))
cat("La probabilidad de que al menos 5 agentes cierren ventas es:",
 probabilidad_al_menos_cinco * 100, "%")

```
8. Un fabricante de juguetes sabe que la probabilidad de que un juguete sea defectuoso es del 5 %. Si se producen
200 juguetes, ¿cuál es la probabilidad de que exactamente 15 sean defectuosos?


```{r }
probabilidad_exito <- 0.05
n <- 200
k <- 15
probabilidad_15_defectuosos <- dbinom(k, n, probabilidad_exito)
cat("La probabilidad de que exactamente 15 juguetes sean defectuosos es:",
 probabilidad_15_defectuosos * 100, "%")

```
9. Un casino sabe que la probabilidad de que un jugador gane en la ruleta es del 47 %. Si un jugador juega 10
rondas, ¿cuál es la probabilidad de que gane al menos 6 rondas?


```{r }
probabilidad_exito <- 0.47
n <- 10
k <- 6
probabilidad_al_menos_seis <- sum(dbinom(k:n, n, probabilidad_exito))
cat("La probabilidad de que el jugador gane al menos 6 rondas es:",
 probabilidad_al_menos_seis * 100, "%")

```

10. Una tienda de ropa sabe que la probabilidad de que un cliente compre un artículo es del 30 %. Si 50 clientes
entran a la tienda, ¿cuál es la probabilidad de que al menos 10 compren algún artículo?

```{r }
probabilidad_exito <- 0.30
n <- 50
k <- 10
probabilidad_al_menos_diez <- (k*100)/(n*probabilidad_exito)
cat("La probabilidad de que al menos 10 clientes compren algun articulo es:",
 probabilidad_al_menos_diez , "%")

```
# 1.1.4. Subsección 1.1.4: Distribución Poisson
1. Una tienda de abarrotes recibe un promedio de 5 clientes por hora. ¿Cuál es la probabilidad de que lleguen
exactamente 10 clientes en las próximas dos horas?

```{r }
lambda <- 5
k <- 10
probabilidad_10_clientes <-probabilidad_10_clientes <- dpois(k, lambda * 2)
cat("La probabilidad de que lleguen exactamente 10 clientes en las 
 proximas dos horas es:", probabilidad_10_clientes * 100, "%")

```
2. Un centro de llamadas recibe en promedio 15 llamadas por hora. ¿Cuál es la probabilidad de que reciban al
menos 20 llamadas en una hora determinada?


```{r }
lambda <- 15
k <- 20
probabilidad_al_menos_20_llamadas <- dpois(k,15)
cat("La probabilidad de que reciban al menos 20 llamadas en una 
 hora determinada es:", probabilidad_al_menos_20_llamadas * 100, "%")

```
3. Un restaurante recibe un promedio de 3 quejas de clientes por día. ¿Cuál es la probabilidad de que reciba
exactamente 2 quejas en un día?


```{r }
lam <- 3
k <- 2
probabilidad_2_quejas <- dpois(k,lam)
cat("La probabilidad de que reciban al menos 2 que al dia  es:", probabilidad_2_quejas * 100, "%")
```
4. Una empresa de envío de paquetes recibe un promedio de 4 paquetes por hora. ¿Cuál es la probabilidad de que
reciban al menos 6 paquetes en una hora determinada?
```{r }
lambda <- 4
k <- 5
probabilidad_al_menos_6_paquetes <- 1 - dpois(k, lambda)
cat("La probabilidad de que la empresa reciba al menos 6 paquetes en
 una hora es:", probabilidad_al_menos_6_paquetes * 100, "%")
```

5. Un médico atiende un promedio de 2 pacientes por hora. ¿Cuál es la probabilidad de que atienda exactamente
5 pacientes en un período de 3 horas?


```{r }
lambda <- 2
k <- 5
probabilidad_5_pacientes <- dpois(k, lambda * 3)
cat("La probabilidad de que el medico atienda exactamente 5 pacientes
 en un periodo de 3 horas es:", probabilidad_5_pacientes * 100, "%")


```
6. Un canal de noticias transmite un promedio de 3 noticias de última hora por día. ¿Cuál es la probabilidad de
que transmitan exactamente 2 noticias de última hora en un día determinado?


```{r }
lambda <- 3
k <- 2
probabilidad_2_noticias <- dpois(k, lambda)
cat("La probabilidad de que el canal de noticias transmita exactamente
 2 noticias de ultima hora en un dia determinado es:", 
 probabilidad_2_noticias * 100, "%")


```

7. Una fábrica produce un promedio de 10 piezas defectuosas por día. ¿Cuál es la probabilidad de que produzcan
al menos 15 piezas defectuosas en un día determinado?

```{r }
lambda <- 10
k <- 15
probabilidad_al_menos_15_defectuosas <- ppois(k , lambda)
cat("La probabilidad de que la fabrica produzca al menos 15 piezas
 defectuosas en un dia determinado es:", 
 probabilidad_al_menos_15_defectuosas * 100, "%")

```
8. Un equipo de fútbol anota en promedio 2 goles por partido. ¿Cuál es la probabilidad de que anoten exactamente
3 goles en un partido determinado?


```{r }
lambda <- 2
k <- 3
probabilidad_exactamente_3_goles <- dpois(k, lambda)
cat("La probabilidad de que el equipo anote exactamente
 3 golesen un partido determinado es:", 
 probabilidad_exactamente_3_goles * 100, "%")

```
9. Un supermercado vende en promedio 20 paquetes de arroz por día. ¿Cuál es la probabilidad de que vendan
menos de 15 paquetes de arroz en un día determinado?



```{r }
lambda <- 20
k <- 15

probabilidad_menos_de_15_paquetes <- ppois(k-1, lambda)
cat("La probabilidad de que el supermercado venda menos de 15 
 paquetes de arroz en un dia determinado es:", 
 probabilidad_menos_de_15_paquetes * 100, "%")

```
10. Una empresa de taxis recibe en promedio 8 solicitudes de viaje por hora. ¿Cuál es la probabilidad de que
reciban al menos 12 solicitudes de viaje en una hora determinada?



```{r }
lambda <- 8
k <- 12
probabilidad_al_menos_12_solicitudes <- 1 - ppois(k, lambda)
cat("La probabilidad de que la empresa de taxis reciba al menos 12
 solicitudes de viaje en una hora determinada es:", 
 probabilidad_al_menos_12_solicitudes * 100, "%")
```
# 1.1.5. Subsección 1.1.5: Distribución Geométrica
1. La probabilidad de que un equipo de baloncesto anote un punto en un tiro libre es del 70 %. La distribución
geométrica se puede usar para calcular la probabilidad de que un jugador necesite más de un intento para
anotar un tiro libre.

```{r }
prob<- pgeom(2,0.7, lower.tail = T)
cat("la probavilidad de que se necesite menos de 2 tiro  es:",prob*100, "%")
```
2. Un sitio web tiene una tasa de conversión del 5 %, lo que significa que el 5 % de los visitantes del sitio web
se convierten en clientes. La distribución geométrica se puede usar para calcular la probabilidad de que se
necesiten más de 10 visitas al sitio web para obtener un cliente.


```{r }
prob<- pgeom(10,0.05, lower.tail = F)
cat("la probavilidad de que se necesite mas de 10 visitas es   es:",prob*100, "%")

```

3. Un restaurante tiene una probabilidad del 20 % de que un cliente ordene un postre. La distribución geométrica
se puede usar para calcular la probabilidad de que se necesiten más de 5 clientes antes de que alguien ordene
un postre.

```{r }
prob<- pgeom(5,0.02, lower.tail = F)
cat("la probavilidad de que se necesite mas de 5 clientes es:",prob*100, "%")
```
4. Un jugador de póker tiene una probabilidad del 2 % de recibir un as en su mano inicial. La distribución
geométrica se puede usar para calcular la probabilidad de que el jugador necesite más de 3 manos para recibir
su primer as.



```{r }
prob<- pgeom(3,0.02, lower.tail = F)
cat("la probavilidad de que se necesite mas de 3 manos es:",prob*100, "%")
```
5. Una empresa de marketing envía correos electrónicos a una lista de clientes potenciales. La probabilidad de
que un cliente potencial abra el correo electrónico es del 10 %. La distribución geométrica se puede usar para
calcular la probabilidad de que se necesiten más de 5 correos electrónicos para que un cliente potencial abra
uno.


```{r }
prob<- pgeom(5,0.1, lower.tail = F)
cat("la probavilidad de que se necesite mas de 5 correos es:",prob*100, "%")
```
6. Un jugador de béisbol tiene una probabilidad del 25 % de conectar una pelota en un turno al bate. La
distribución geométrica se puede usar para calcular la probabilidad de que el jugador necesite más de 4 turnos
al bate para conectar su primer hit


```{r }
prob<- pgeom(4,0.25, lower.tail = F)
cat("la probavilidad de que se necesite mas de 4 turnos es:",prob*100, "%")
```
7. Un vendedor tiene una probabilidad del 30 % de cerrar una venta en una llamada de ventas. La distribución
geométrica se puede usar para calcular la probabilidad de que el vendedor necesite más de 6 llamadas de ventas
para cerrar su primera venta


```{r }
prob<- pgeom(6,0.30, lower.tail = F)
cat("la probavilidad de que se necesite mas de 6 llamadas es:",prob*100, "%")
```
8. La probabilidad de que un estudiante pase un examen es del 60 %. La distribución geométrica se puede usar
para calcular la probabilidad de que el estudiante necesite más de 4 intentos para pasar el examen.


```{r }
prob<- pgeom(4,0.60, lower.tail = F)
cat("la probavilidad de que se necesite mas de 4 de intentos es:",prob*100, "%")
```
9. Un jugador de fútbol americano tiene una probabilidad del 5 % de interceptar un pase en un juego. La
distribución geométrica se puede usar para calcular la probabilidad de que el jugador necesite más de 10
intentos para interceptar su primer pase.



```{r }
prob<- pgeom(10,0.05, lower.tail = F)
cat("la probavilidad de que se necesite mas de 10 de intentos es:",prob*100, "%")
```
10. Un pescador tiene una probabilidad del 10 % de capturar un pez en cada lance de su caña de pesca. La
distribución geométrica se puede usar para calcular la probabilidad de que el pescador necesite más de 8 lances
para capturar su primer pez


```{r }
prob<- pgeom(8,0.10, lower.tail = F)
cat("la probavilidad de que se necesite mas de 8 lances es:",prob*100, "%")
```
## 1.2. Sección 1.2: Variables Aleatorias Continuas
#1.2.1. Subsección 1.2.1: Distribución Uniforme
1. Un fabricante de piezas mecánicas sabe que la longitud de sus piezas puede variar en un rango de 10 a 20 cm.
La variable aleatoria uniforme continua se puede usar para modelar la longitud de las piezas que producen.

```{r }
#muestras de 100

n <- 100
a <- 10
b <- 20
piezas <- runif(n, min = a, max = b)
# Calcular la probabilidad de que una pieza tenga una longitud 
# mayor a 15 cm
prob_mayor_15 <- 1 - punif(15, min = a, max = b)
# Calcular la probabilidad de que una pieza tenga una longitud 
# entre 12 y 18 cm
prob_entre_12_18 <- diff(punif(c(12, 18), min = a, max = b))
# Calcular la longitud esperada de las piezas
longitud_esperada <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de longitud mayor a 15 cm:", prob_mayor_15, "\n")
cat("Probabilidad de longitud entre 12 y 18 cm:", prob_entre_12_18, "\n")
cat("Longitud esperada de las piezas:", longitud_esperada, "cm", "\n")



```

2. Un negocio de alquiler de bicicletas sabe que el tiempo que los clientes alquilan sus bicicletas varía entre 1 y 4
horas. La variable aleatoria uniforme continua se puede usar para modelar la duración de los alquileres.

```{r }
#muestras de 100
n <- 100
a <- 1
b <- 4
duraciones <- runif(n, min = a, max = b)
# Calcular la probabilidad de que un alquiler dure menos de 2 horas
prob_menor_2 <- punif(2, min = a, max = b)
# Calcular la probabilidad de que un alquiler dure entre 1.5 y 3.5 horas
prob_entre_1.5_3.5 <- diff(punif(c(1.5, 3.5), min = a, max = b))
# Calcular la duracion promedio de los alquileres
duracion_promedio <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de duracion menor a 2 horas:", 
 prob_menor_2, "\n")


```
3. Un restaurante sabe que la cantidad de tiempo que los clientes pasan en el restaurante varía entre 30 minutos
y 2 horas. La variable aleatoria uniforme continua se puede usar para modelar el tiempo de estancia de los
clientes.



```{r }
#usemos 100 muestras
n <- 100
a <- 30
b <- 120
tiempos_estancia <- runif(n, min = a, max = b)
# Calcular la probabilidad de que un cliente pase menos de 1 hora
# en el restaurante
prob_menor_1_hora <- punif(60, min = a, max = b)
# Calcular la probabilidad de que un cliente pase entre 45 
# minutos y 1.5 horas en el restaurante
prob_entre_45min_1.5h <- diff(punif(c(45, 90), min = a, max = b))
# Calcular el tiempo de estancia promedio de los clientes
tiempo_estancia_promedio <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de tiempo de estancia menor a 1 hora:", 
 prob_menor_1_hora, "\n")
cat("Probabilidad de tiempo de estancia entre 45 minutos y 1.5 horas:", 
 prob_entre_45min_1.5h, "\n")
cat("Tiempo de estancia promedio de los clientes:",
 tiempo_estancia_promedio, "minutos", "\n")

```
4. Una compañía de seguros sabe que la velocidad de los automóviles en una carretera puede variar entre 60 y
120 km/h. La variable aleatoria uniforme continua se puede usar para modelar la velocidad de los vehículos en
la carretera.



```{r }
#muestras d 100
n <- 1000
a <- 60
b <- 120
velocidades <- runif(n, min = a, max = b)
# Calcular la probabilidad de que un vehiculo tenga una 
# velocidad mayor a 80 km/h
prob_mayor_80 <- 1 - punif(80, min = a, max = b)
# Calcular la probabilidad de que un vehiculo tenga una 
# velocidad entre 70 y 90 km/h
prob_entre_70_90 <- diff(punif(c(70, 90), min = a, max = b))
# Calcular el valor esperado de la velocidad de los vehiculos
velocidad_promedio <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de velocidad mayor a 80 km/h:", 
 prob_mayor_80, "\n")
cat("Probabilidad de velocidad entre 70 y 90 km/h:", 
 prob_entre_70_90, "\n")
cat("Velocidad promedio de los vehiculos:", 
 velocidad_promedio, "km/h", "\n")


```
5. Un fabricante de ropa sabe que el peso de las camisetas que produce puede variar entre 100 y 200 gramos. La
variable aleatoria uniforme continua se puede usar para modelar el peso de las camisetas.



```{r }
#muestras de 100
n <- 100
a <- 100
b <- 200
pesos_camisetas <- runif(n, min = a, max = b)
# Calcular la probabilidad de que una camiseta tenga un peso
# mayor a 150 gramos
prob_mayor_150 <- 1 - punif(150, min = a, max = b)
# Calcular la probabilidad de que una camiseta tenga un peso 
# entre 120 y 180 gramos
prob_entre_120_180 <- diff(punif(c(120, 180), min = a, max = b))
# Calcular el peso promedio de las camisetas
peso_promedio <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de peso mayor a 150 gramos:", 
 prob_mayor_150, "\n")
cat("Probabilidad de peso entre 120 y 180 gramos:", 
 prob_entre_120_180, "\n")
cat("Peso promedio de las camisetas:", 
 peso_promedio, "gramos", "\n")


```
6. Una tienda de electrónica sabe que el voltaje que suministra su fuente de alimentación puede variar entre 110
y 220 voltios. La variable aleatoria uniforme continua se puede usar para modelar el voltaje de la fuente de
alimentación.


```{r }
n <- 100
a <- 110
b <- 220
voltajes <- runif(n, min = a, max = b)
# Calcular la probabilidad de que el voltaje sea mayor a 180 voltios
prob_mayor_180 <- 1 - punif(180, min = a, max = b)
# Calcular la probabilidad de que el voltaje este entre 150 y 200 voltios
prob_entre_150_200 <- diff(punif(c(150, 200), min = a, max = b))
# Calcular el valor esperado del voltaje de la fuente de alimentacion
valor_esperado <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de voltaje mayor a 180 voltios:", 
 prob_mayor_180, "\n")
cat("Probabilidad de voltaje entre 150 y 200 voltios:", 
 prob_entre_150_200, "\n")
cat("Valor esperado del voltaje de la fuente de alimentacion:", 
 valor_esperado, "voltios", "\n")
```
7. Un servicio de mensajería sabe que la distancia entre los puntos de recogida y entrega puede variar entre 5 y
20 km. La variable aleatoria uniforme continua se puede usar para modelar la distancia de entrega.


```{r }
#muetras de 100
n <- 100
a <- 5
b <- 20
distancias <- runif(n, min = a, max = b)
# Calcular la probabilidad de que la distancia sea menor a 10 km
prob_menor_10 <- punif(10, min = a, max = b)
# Calcular la probabilidad de que la distancia este entre 15 y 18 km
prob_entre_15_18 <- diff(punif(c(15, 18), min = a, max = b))
# Calcular el valor esperado de la distancia de entrega
valor_esperado <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de distancia menor a 10 km:", 
 prob_menor_10, "\n")
cat("Probabilidad de distancia entre 15 y 18 km:", 
 prob_entre_15_18, "\n")
cat("Valor esperado de la distancia de entrega:", 
 valor_esperado, "km", "\n")
```
8. Un fabricante de televisores sabe que el tiempo que tardan sus empleados en ensamblar un televisor puede
variar entre 1 y 3 horas. La variable aleatoria uniforme continua se puede usar para modelar el tiempo de
ensamblaje.


```{r }
n <- 100
a <- 1
b <- 3
tiempos_ensamblaje <- runif(n, min = a, max = b)
# Calcular la probabilidad de que el tiempo de ensamblaje sea menor a 2 horas
prob_menor_2_horas <- punif(2, min = a, max = b)
# Calcular la probabilidad de que el tiempo de ensamblaje este entre 
# 1.5 y 2.5 horas
prob_entre_1.5_2.5 <- diff(punif(c(1.5, 2.5), min = a, max = b))
# Calcular el valor esperado del tiempo de ensamblaje
valor_esperado <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de tiempo de ensamblaje menor a 2 horas:", 
 prob_menor_2_horas, "\n")
cat("Probabilidad de tiempo de ensamblaje entre 1.5 y 2.5 horas:", 
 prob_entre_1.5_2.5, "\n")
cat("Valor esperado del tiempo de ensamblaje:", valor_esperado, "horas", "\n")
```
9. Una empresa de telecomunicaciones sabe que la velocidad de descarga de su red puede variar entre 10 y 50
Mbps. La variable aleatoria uniforme continua se puede usar para modelar la velocidad de descarga de la red.



```{r }
n <- 1000
a <- 10
b <- 50
velocidades_descarga <- runif(n, min = a, max = b)
# Calcular la probabilidad de que la velocidad de descarga sea mayor a 30 Mbps
prob_mayor_30_Mbps <- 1 - punif(30, min = a, max = b)
# Calcular la probabilidad de que la velocidad de descarga este entre 20 y 
# 40 Mbps
prob_entre_20_40_Mbps <- diff(punif(c(20, 40), min = a, max = b))
# Calcular el valor esperado de la velocidad de descarga
valor_esperado <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de velocidad de descarga mayor a 30 Mbps:", 
 prob_mayor_30_Mbps, "\n")
cat("Probabilidad de velocidad de descarga entre 20 y 40 Mbps:",
 prob_entre_20_40_Mbps, "\n")
cat("Valor esperado de la velocidad de descarga:", 
 valor_esperado, "Mbps", "\n")


```
10. Un concesionario de autos sabe que el precio de sus automóviles puede variar entre $10,000 y $30,000. La
variable aleatoria uniforme continua se puede usar para modelar el precio de los automóviles en el concesionario.


```{r }
n <- 100
a <- 10000
b <- 30000
precios_automoviles <- runif(n, min = a, max = b)
# Calcular la probabilidad de que el precio de un automovil sea mayor a 
# $20,000
prob_mayor_20000 <- 1 - punif(20000, min = a, max = b)
# Calcular la probabilidad de que el precio de un automovil este entre 
# $15,000 y $25,000
prob_entre_15000_25000 <- diff(punif(c(15000, 25000), min = a, max = b))
# Calcular el valor esperado del precio de los automoviles
valor_esperado <- (a + b) / 2
# Imprimir los resultados
cat("Probabilidad de precio de automovil mayor a $20,000:", 
 prob_mayor_20000, "\n")
cat("Probabilidad de precio de automovil entre $15,000 y $25,000:",
 prob_entre_15000_25000, "\n")
cat("Valor esperado del precio de los automoviles: $", 
 valor_esperado, "\n")

```
# 1.2.2. Subsección 
1.2.2: Distribución Exponencial
1. Una empresa de mensajería promete entregar los paquetes dentro de 2 horas desde que se realiza el pedido. El
tiempo que tarda en entregarse un paquete sigue una distribución exponencial con una tasa de 0.5. ¿Cuál es la
probabilidad de que un paquete se entregue en menos de 1 hora?


```{r }
lambda <- 0.5 
x <- 1
probabilidad <- 1 - exp(-lambda * x)
probabilidad*100

```
2. La vida útil de las bombillas de una fábrica sigue una distribución exponencial con una media de 800 horas.
¿Cuál es la probabilidad de que una bombilla dure más de 1000 horas?


```{r }
media <- 800
tiempo <- 1000
probabilidad <- 1 - pexp(tiempo, rate = 1/media)
probabilidad*100

```

3. Un sistema informático tiene una tasa de fallos de 0.01 por hora. ¿Cuál es la probabilidad de que el sistema
falle en menos de 50 horas?

```{r }
tasa_fallos <- 0.01
tiempo <- 50
probabilidad <- pexp(tiempo, rate = tasa_fallos)
probabilidad*100
```
4. Un vendedor tarda en promedio 15 minutos en atender a un cliente en su tienda. El tiempo que tarda en
atender a los clientes sigue una distribución exponencial con una tasa de 0.25. ¿Cuál es la probabilidad de que
atienda a un cliente en menos de 10 minutos?


```{r }
tasa_atencion <- 0.25
tiempo <- 10
probabilidad <- pexp(tiempo, rate = tasa_atencion)
probabilidad*100
```

5. El tiempo entre llegadas de dos clientes a una tienda sigue una distribución exponencial con una media de 5
minutos. ¿Cuál es la probabilidad de que no llegue ningún cliente en 10 minutos?


```{r }
tasa_llegadas <- 0.2
tiempo <- 10
probabilidad <-  exp(tasa_llegadas * tiempo)
probabilidad*100

```

6. El tiempo que tarda una persona en recorrer una distancia de 5 km en bicicleta sigue una distribución
exponencial con una media de 30 minutos. ¿Cuál es la probabilidad de que la persona tarde menos de 20
minutos?


```{r }
tasa_llegadas <- 0.0333
tiempo <- 20
probabilidad <- exp(-tasa_llegadas * tiempo)
probabilidad*100
```
7. El tiempo que tarda una máquina en producir una pieza sigue una distribución exponencial con una tasa
de 0.003 piezas por minuto. ¿Cuál es la probabilidad de que la máquina produzca al menos 10 piezas en 5
minutos?



```{r }
tasa_produccion <- 0.003
tiempo <- 5
probabilidad <- exp(-tasa_produccion * tiempo)
probabilidad*100
```
8. El tiempo que tarda un proceso industrial en completarse sigue una distribución exponencial con una media de
8 horas. ¿Cuál es la probabilidad de que el proceso tarde más de 10 horas en completarse?


```{r }
media <- 8
tasa <- 1/media
tiempo <- 10
probabilidad <- exp(-tasa * tiempo)
probabilidad*100

```
9. El tiempo que tarda un cliente en pagar en una caja sigue una distribución exponencial con una media de 2
minutos. Si hay 10 clientes esperando en la fila, ¿cuál es la probabilidad de que un cliente nuevo tenga que
esperar más de 5 minutos para pagar?


```{r }
media <- 2
tasa <- 1/media
tiempo <- 5
probabilidad <- exp(-tasa * tiempo)
probabilidad*100

```

10. La tasa de llegada de un evento de demanda en un centro de atención telefónica sigue una distribución
exponencial con una media de 4 eventos por hora. ¿Cuál es la probabilidad de que no llegue ningún evento en
10 minutos?

```{r }
tasa <- 4/60 
tiempo <- 10 
probabilidad <- pexp(tiempo, rate = tasa) - pexp(0, rate = tasa)
probabilidad

```
11. El tiempo que tarda una persona en llegar a una estación de tren sigue una distribución exponencial con una
media de 20 minutos. Si una persona tiene que tomar un tren que sale en 30 minutos, ¿cuál es la probabilidad
de que llegue a tiempo?



```{r }
media <- 20 # Media en minutos
t <- 30 # Tiempo en minutos
tasa <- 1/media # Tasa de la distribucion exponencial
probabilidad <- exp(-tasa * t)
probabilidad*100

```
12. El tiempo que tarda un coche en recorrer una distancia de 100 km en una carretera sigue una distribución
exponencial con una media de 2 horas. ¿Cuál es la probabilidad de que el coche tarde menos de 1 hora y
media?


```{r }
media <- 2 
t <- 1.5 
distancia <- 100 
velocidad_media <- distancia / media 
velocidad_media_mins <- velocidad_media * 60 
tasa <- 1/velocidad_media_mins 
probabilidad <- pexp(t, rate = tasa)
probabilidad*100
```

13. Se sabe que el tiempo que tarda un técnico en reparar un electrodoméstico sigue una distribución exponencial
con una tasa de 0.05. ¿Cuál es la probabilidad de que el técnico repare el electrodoméstico en menos de 20
minutos?


```{r }
tasa <- 0.05 
tiempo <- 20 
probabilidad <- pexp(tiempo, rate = tasa)
probabilidad*100

```
14. Una empresa de mensajería sabe que el tiempo que tarda en entregar un paquete sigue una distribución
exponencial con una tasa de 0.1. Si se tienen que entregar 100 paquetes, ¿cuál es la probabilidad de que la
empresa tarde menos de 30 minutos en entregar cada paquete?


```{r }
tasa <- 0.1 
tiempo <- 30 
num_paquetes <- 100
probabilidad <- 0
for (i in 1:num_paquetes) {
 probabilidad <- probabilidad + pexp(tiempo, rate = tasa)
}
probabilidad

```

15. Se sabe que el tiempo que tarda en atenderse un cliente en una tienda sigue una distribución exponencial con
una tasa de 0.2. ¿Cuál es la probabilidad de que un cliente espere más de 10 minutos para ser atendido?


```{r }
tasa <- 0.2 
tiempo <- 10
probabilidad <- 1 - pexp(tiempo, rate = tasa)
probabilidad*100

```

16. Un fabricante de baterías sabe que la duración de sus baterías sigue una distribución exponencial con una tasa
de 0.01. Si se venden 500 baterías, ¿cuál es la probabilidad de que al menos 10 de ellas duren más de 100 horas?


```{r }
tasa <- 0.01 
n_baterias <- 500 
n_duran_mas_100h <- 10 

probabilidad <- 1 - pexp(100, rate = tasa)^n_duran_mas_100h
probabilidad*100

```

17. Un vendedor de autos usados sabe que la cantidad de tiempo que tarda en vender un auto sigue una distribución
exponencial con una tasa de 0.1. Si tiene 20 autos en venta, ¿cuál es la probabilidad de que tarde más de 5
horas en venderlos todos?

```{r }
tasa <- 0.1 
n_autos <- 20
tiempo_limite <- 5 
probabilidad <- 1 - pexp(tiempo_limite, rate = tasa)^n_autos
probabilidad*100

```

18. Se sabe que la cantidad de tiempo que tarda una persona en caminar desde su casa hasta la estación de
tren sigue una distribución exponencial con una tasa de 0.05. Si la distancia es de 1 kilómetro, ¿cuál es la
probabilidad de que la persona tarde menos de 20 minutos en llegar a la estación?

```{r }
tasa <- 0.05 
distancia <- 1 
tiempo_limite <- 20 
probabilidad <- pexp(tiempo_limite, rate = tasa*distancia)
probabilidad*100

```
19. Un banco sabe que la cantidad de tiempo que tarda un cliente en realizar una transacción sigue una distribución
exponencial con una tasa de 0.15. Si se tienen que atender a 50 clientes, ¿cuál es la probabilidad de que el
banco tarde menos de 2 horas en atenderlos a todos?



```{r }
tasa <- 0.15 
num_clientes <- 50 
tiempo_limite <- 2
probabilidad <- pexp(tiempo_limite*60, rate = tasa*num_clientes)
probabilidad*100
```
20. Se sabe que la cantidad de tiempo que tarda en fallar un equipo electrónico sigue una distribución exponencial
con una tasa de 0.02. Si se tienen 100 equipos en operación, ¿cuál es la probabilidad de que al menos 5 fallen
en menos de 50 horas?



```{r }
tasa <- 0.02 
num_equipos <- 100 
tiempo_limite <- 50 
num_fallan <- 5 
probabilidad <- 1 - pexp(tiempo_limite, rate = tasa*num_equipos) -
 sum(dexp(seq(0, tiempo_limite), rate = tasa*num_equipos, log = TRUE) *
 choose(num_equipos, num_fallan) *
 (pexp(seq(0, tiempo_limite), rate = tasa))^num_fallan)
probabilidad
```
21. Un restaurante sabe que la cantidad de tiempo que tarda un cliente en terminar de comer sigue una distribución
exponencial con una tasa de 0.2. Si se tienen 30 mesas, ¿cuál es la probabilidad de que al menos 5 de ellas
estén desocupadas en menos de 1 hora?


```{r }

```
22. Un servidor web sabe que la cantidad de tiempo que tarda en procesar una solicitud sigue una distribución
exponencial con una tasa de 0.05. Si recibe 100 solicitudes, ¿cuál es la probabilidad de que tarde menos de 10
minutos en procesar cada una?


```{r }
tasa <- 0.05 # Tasa de la distribucion exponencial
num_solicitudes <- 100 # Numero de solicitudes
tiempo_limite <- 10 # Tiempo limite en minutos
probabilidad <- pexp(tiempo_limite, rate = tasa)^num_solicitudes
probabilidad
```
#1.2.3. Subsección 1.2.3: Distribución Normal
1. Una fábrica de papel produce rollos de papel higiénico que tienen una longitud media de 100 metros y una
desviación estándar de 5 metros. ¿Cuál es la probabilidad de que un rollo de papel higiénico tenga una longitud
entre 95 y 105 metros?


```{r }
probabilidad <- pnorm(105, mean = 100, sd = 5) - pnorm(95, mean = 100, sd = 5)
probabilidad*100

```
2. La altura de los estudiantes de una escuela secundaria sigue una distribución normal con media de 165 cm y
desviación estándar de 10 cm. ¿Cuál es la probabilidad de que un estudiante seleccionado al azar tenga una altura entre 150 y 180 cm?



```{r }
robabilidad <- pnorm(180, mean = 165, sd = 10) - pnorm(150, mean = 165,
 sd = 10)
probabilidad*100
```
3. Un fabricante de zapatos produce zapatos que tienen una longitud media de 28 cm y una desviación estándar
de 2 cm. ¿Cuál es la probabilidad de que un zapato seleccionado al azar tenga una longitud mayor que 32 cm?



```{r }
probabilidad <- 1 - pnorm(32, mean = 28, sd = 2)
probabilidad*100

```
4. La distribución de las calificaciones en un examen sigue una distribución normal con una media de 70 y una
desviación estándar de 10. ¿Cuál es la probabilidad de que un estudiante seleccionado al azar tenga una
calificación menor a 60?


```{r }
probabilidad <- pnorm(60, mean = 70, sd = 10)
probabilidad*100

```
5. La duración de las baterías de un dispositivo electrónico sigue una distribución normal con una media de 10
horas y una desviación estándar de 2 horas. ¿Cuál es la probabilidad de que una batería seleccionada al azar
dure más de 12 horas?


```{r }
probabilidad <- 1 - pnorm(12, mean = 10, sd = 2)
probabilidad*100
```
6. La distribución de los tiempos de espera en una fila sigue una distribución normal con una media de 5 minutos
y una desviación estándar de 1 minuto. ¿Cuál es la probabilidad de que un cliente tenga que esperar más de 8
minutos?


```{r }
probabilidad <- 1 - pnorm(8, mean = 5, sd = 1)
probabilidad*100

```

7. El peso de los recién nacidos sigue una distribución normal con una media de 3.5 kg y una desviación estándar
de 0.5 kg. ¿Cuál es la probabilidad de que un recién nacido pese menos de 2.5 kg?


```{r }
probabilidad <- pnorm(2.5, mean = 3.5, sd = 0.5)
probabilidad*100

```
8. La velocidad de un vehículo sigue una distribución normal con una media de 60 km/h y una desviación estándar
de 5 km/h. ¿Cuál es la probabilidad de que un vehículo viaje a una velocidad mayor de 70 km/h?


```{r }
probabilidad <- 1 - pnorm(70, mean = 60, sd = 5)
probabilidad*100

```
9. La distribución de los ingresos de los trabajadores en una empresa sigue una distribución normal con una
media de $50, 000 y una desviación estándar de $10, 000. ¿Cuál es la probabilidad de que un trabajador gane
menos de $30, 000?


```{r }
probabilidad <- 1-pnorm(30000, mean = 50000, sd = 10000)
probabilidad*100

```

10. La temperatura de una ciudad sigue una distribución normal con una media de 25 grados Celsius y una
desviación estándar de 2 grados Celsius. ¿Cuál es la probabilidad de que la temperatura sea mayor de 30
grados Celsius?

```{r }
probabilidad <- 1 - pnorm(30, mean = 25, sd = 2)
probabilidad*100

```

11. La altura de las botellas de refresco sigue una distribución normal con una media de 20 cm y una desviación
estándar de 1 cm. ¿Cuál es la probabilidad de que una botella seleccionada al azar tenga una altura menor a
18 cm?
```{r }
probabilidad <- 1-pnorm(18, mean = 20, sd = 1)
probabilidad*100

```

12. Si los puntajes de un examen están distribuidos normalmente con una media de 70 y una desviación estándar
de 10, ¿cuál es la probabilidad de obtener un puntaje mayor a 80?

```{r }
robabilidad <- pnorm(80, mean = 70, sd = 10)
probabilidad*100

```
13. Suponga que el tiempo que se tarda en atender a un cliente en un banco sigue una distribución normal con
media de 5 minutos y una desviación estándar de 1.2 minutos. ¿Cuál es la probabilidad de que un cliente sea
atendido en menos de 4 minutos?


```{r }
probabilidad <- pnorm(4, mean = 5, sd = 1.2)
probabilidad*100

```
14. Un estudio indica que el peso promedio de los estudiantes de una universidad es de 70 kg con una desviación
estándar de 8 kg. Si se selecciona una muestra aleatoria de 25 estudiantes, ¿cuál es la probabilidad de que el
peso promedio de la muestra sea mayor a 75 kg?


```{r }
probabilidad <- 1 - pnorm(75, mean = 70, sd = 8/sqrt(25))
probabilidad*100

```
15. Suponga que la altura de los jugadores de un equipo de baloncesto se distribuye normalmente con una media
de 200 cm y una desviación estándar de 10 cm. Si se selecciona un jugador al azar, ¿cuál es la probabilidad de
que su altura sea mayor a 215 cm?



```{r }
probabilidad <- 1 - pnorm(215, mean = 200, sd = 10)
probabilidad*100

```
16. Si los ingresos mensuales de los empleados de una empresa están distribuidos normalmente con una media
de $2500 y una desviación estándar de $500, ¿cuál es la probabilidad de que un empleado tenga un ingreso
mensual mayor a $3000?


```{r }
probabilidad <- 1 - dnorm(3000, mean = 2500, sd = 500)
probabilidad*100
```
17. La cantidad de días que tarda una empresa en pagar a sus proveedores sigue una distribución normal con
una media de 30 días y una desviación estándar de 5 días. Si se selecciona un proveedor al azar, ¿cuál es la
probabilidad de que la empresa le pague en menos de 25 días?



```{r }
probabilidad <- pnorm(25, mean = 30, sd = 5)
probabilidad*100
```
18. Suponga que el diámetro de los tornillos producidos por una fábrica sigue una distribución normal con media
de 4 cm y una desviación estándar de 0.2 cm. Si se selecciona un tornillo al azar, ¿cuál es la probabilidad de
que su diámetro esté entre 3.8 cm y 4.2 cm?


```{r }
probabilidad <- pnorm(4.2, mean = 4, sd = 0.2) - pnorm(3.8, mean = 4, sd = 0.2)
probabilidad*100

```
19. El tiempo que tarda un trabajador en completar una tarea sigue una distribución normal con media de 6 horas
y una desviación estándar de 1 hora. Si se selecciona un trabajador al azar, ¿cuál es la probabilidad de que
complete la tarea en menos de 4 horas?


```{r }
probabilidad <- pnorm(4, mean = 6, sd = 1)
probabilidad*100

```

20. Si la cantidad de gasolina que se llena en un tanque de un automóvil sigue una distribución normal con una
media de 50 litros y una desviación estándar de 5 litros, ¿cuál es la probabilidad de que se llenen entre 45 y 55
litros?

```{r }
probabilidad <- pnorm(55, mean = 50, sd = 5) - pnorm(45, mean = 50, sd = 5)
probabilidad*100
```

21. El peso de los paquetes que se envían por una compañía de paquetería sigue una distribución normal con una
media de 2 kg y una desviación estándar de 0.5 kg. Si se selecciona un paquete al azar, ¿cuál es la probabilidad
de que su peso esté entre 1.5 kg y 2.5 kg?

```{r }
robabilidad <- pnorm(2.5, mean = 2, sd = 0.5) - dnorm(1.5, mean = 2, sd = 0.5)
probabilidad*100

```

# 1.2.4. Subsección 1.2.4: Distribución Gamma
1. Un fabricante de bombillas sabe que la vida útil de las bombillas sigue una distribución gamma con una media
de 1500 horas y una desviación estándar de 500 horas. ¿Cuál es la probabilidad de que una bombilla dure más
de 2000 horas?

```{r }
probabilidad <- 1 - pgamma(2000, shape=1500, scale=1/500)
probabilidad*100
```
2. Una planta de producción de papel produce rollos de papel de 100 metros de longitud. La distribución de la
longitud de los rollos sigue una distribución gamma con una media de 105 metros y una desviación estándar
de 10 metros. ¿Cuál es la probabilidad de que un rollo tenga una longitud de entre 90 y 110 metros?



```{r }
probabilidad<- pgamma(110,shape=100,scale=1/10)-pgamma(90,100,1/10)
probabilidad*100
```

3. La cantidad de tiempo que un grupo de estudiantes dedica a estudiar para un examen sigue una distribución
gamma con una media de 20 horas y una desviación estándar de 5 horas. Si el tiempo mínimo requerido para
pasar el examen es de 15 horas, ¿cuál es la probabilidad de que un estudiante al azar pase el examen?


```{r }
probabilidad <- 1 - pgamma(15, shape = 20, scale = 1/5)
probabilidad*100
```
4. Un fabricante de baterías de automóvil sabe que el tiempo de vida de las baterías sigue una distribución
gamma con una media de 4 años y una desviación estándar de 1 año. ¿Cuál es la probabilidad de que una
batería dure menos de 2 años?



```{r }
probabilidad <- pgamma(2, shape = 4, scale = 1/1)
probabilidad*100
```
5. La cantidad de tiempo que una persona tarda en completar una tarea sigue una distribución gamma con una
media de 10 minutos y una desviación estándar de 2 minutos. Si se requiere que la tarea se complete en un
tiempo máximo de 12 minutos, ¿cuál es la probabilidad de que una persona al azar pueda completar la tarea a
tiempo?



```{r }
probabilidad <- pgamma(12, shape = 10, scale = 1/2)
probabilidad*100
```
6. Una fábrica de muebles produce sillas de madera. La cantidad de tiempo que tarda en producir una silla sigue
una distribución gamma con una media de 2 horas y una desviación estándar de 0.5 horas. Si se requiere que
se produzcan 10 sillas en un día de trabajo, ¿cuál es la probabilidad de que se logre completar la producción
en un día?



```{r }
probabilidad <- pgamma(10, shape = 2, scale = 1/0.5)
probabilidad*100
```

7. Un fabricante de relojes sabe que la vida útil de sus relojes sigue una distribución gamma con una media de 5
años y una desviación estándar de 1 año. Si un reloj es considerado defectuoso si dura menos de 3 años, ¿cuál
es la probabilidad de que un reloj al azar sea considerado defectuoso?



```{r }
probabilidad <- pgamma(3, shape = 5, scale = 1/1)
probabilidad*100
```

8. Un fabricante de focos sabe que la vida útil de sus focos sigue una distribución gamma con una media de 800
horas y una desviación estándar de 200 horas. Si se requiere que los focos duren al menos 1000 horas, ¿cuál es
la probabilidad de que un foco al azar dure el tiempo necesario?


```{r }
probabilidad <- pgamma(1000, shape = 800, scale = 1/200)
probabilidad*100
```
9. La cantidad de tiempo que tarda un técnico en reparar un dispositivo electrónico sigue una distribución gamma
con una media de 3 horas y una desviación estándar de 0.5 horas. Si se requiere que el dispositivo sea reparado
en un tiempo máximo de 4 horas, ¿cuál es la probabilidad de que el técnico al azar pueda reparar el dispositivo
a tiempo?




```{r }
probabilidad <- pgamma(4, shape = 3, scale = 1/0.5)
probabilidad * 100
```
10. Se sabe que el tiempo de vida (en años) de una lámpara sigue una distribución gamma con parámetros α = 3
y β = 2. Encuentra la probabilidad de que la lámpara dure más de 4 años.




```{r }
probabilidad <- (1 - pgamma(4, shape = 3, scale =1/ 2)) 
probabilidad*100
```
11. Un servicio de reparación de electrodomésticos tiene un promedio de 10 reparaciones diarias con una desviación
estándar de 2 reparaciones. Si se modela el número de reparaciones diarias como una distribución gamma,
¿cuál es el valor del parámetro α?



```{r }
med <- 10
desv<- 2
var <- desv^2
alfa <- (med^2) / var
alfa
```
12. El tiempo que tarda en llegar un cliente al restaurante sigue una distribución gamma con parámetros α = 2 y
β = 3. ¿Cuál es la probabilidad de que un cliente tarde más de 5 minutos en llegar?




```{r }
alpha <- 2
beta <- 3
probabilidad <- 1 - pgamma(5, shape = alfa, scale =1/beta)
probabilidad * 100
```

13. Se sabe que la cantidad de kilómetros que recorre un taxi en un día sigue una distribución gamma con
parámetros α = 4 y β = 6. ¿Cuál es la probabilidad de que el taxi recorra menos de 20 kilómetros en un día?



```{r }
alfa <- 4
beta <- 6
probabilidad <- pgamma(20, shape = alfa, scale =1/ beta)
probabilidad * 100
```
14. El tiempo que tarda un empleado en resolver una tarea sigue una distribución gamma con parámetros α = 5 y
β = 2. ¿Cuál es el valor esperado del tiempo de resolución de la tarea?



```{r }
alfa <- 5
beta <- 2
valor <- alfa /(1/beta)
valor
```

15. Un grupo de personas está siendo tratado con un medicamento cuyos efectos secundarios siguen una distribución
gamma con parámetros α = 2 y β = 4. ¿Cuál es la probabilidad de que el efecto secundario más duradero dure
más de 6 horas?


```{r }
alfa <- 2
beta <- 4
probabilidad <-1- pgamma(6, shape = alfa,  rate= 1/beta)
probabilidad * 100
```

16. La cantidad de tiempo que tarda una máquina en fabricar una pieza sigue una distribución gamma con
parámetros α = 4 y β = 5. ¿Cuál es la probabilidad de que la máquina tarde menos de 10 minutos en fabricar
una pieza?


```{r }
alfa <- 4
beta <- 5
probabilidad <- pgamma(10, shape = alfa, rate = 1/beta)
probabilidad * 100
```

17. Se sabe que la cantidad de kilómetros que puede recorrer un coche con un tanque de gasolina sigue una
distribución gamma con parámetros α = 3 y β = 8. Si el coche tiene un tanque de gasolina de 40 litros, ¿cuál
es la probabilidad de que pueda recorrer más de 600 kilómetros con un tanque lleno?


```{r }
alfa <- 3
beta <- 8
probabilidad <- 1 - pgamma(600, shape = alfa, scale = 1/beta)
probabilidad * 100
```
18. El tiempo que tarda un equipo de trabajo en completar un proyecto sigue una distribución gamma con
parámetros α = 6 y β = 3. ¿Cuál es la varianza del tiempo de completación del proyecto?



```{r }
alpha <- 6
beta <- 3
varianza <- alpha / (1/beta)
varianza
```
19. Se sabe que el tiempo que tarda en salir un autobús desde la terminal sigue una distribución gamma con
parámetros α = 3 y β = 2. ¿Cuál es la probabilidad de que el autobús tarde más de 5 minutos en salir?



```{r }
alpha <- 3
beta <- 2
probabilidad <- 1 - pgamma(5, shape = alpha, rate = 1/beta)
probabilidad*100
```

20. La cantidad de tiempo que tarda un paquete en ser entregado sigue una distribución gamma con parámetros
α = 5 y β = 4. ¿Cuál es la probabilidad de que el paquete sea entregado antes de 25 minutos?


```{r }
alpha <- 5
beta <- 4
probabilidad <- pgamma(25, shape = alpha, rate = 1/beta)
probabilidad*100
```
21. Un proceso de producción tiene un promedio de 10 piezas producidas por hora con una desviación estándar de
2 piezas. Si se modela el número de piezas producidas por hora como una distribución gamma, ¿cuál es el valor
del parámetro β



```{r }
prom <- 10
desv <- 2
beta <- prom / (1/desv)
beta
```
# 1.2.5. Subsección 1.2.5: Distribución Beta

1. Un sitio web de videos desea medir la tasa de clics en su botón de “reproducir”. Si se sabe que el 10 % de los
usuarios hacen clic en el botón, ¿cuál es la probabilidad de que de 20 usuarios, exactamente 3 hagan clic?


```{r }
ut <- 20
cli <- 3
probacl <- 0.1
probabilidad<- dbinom(cli, ut, probacl)
probabilidad*100
```

2. Una empresa quiere saber la probabilidad de que un producto defectuoso se encuentre en el proceso de
producción. Si se sabe que la tasa de producción de productos defectuosos es del 5 %, ¿cuál es la probabilidad
de que de 200 productos producidos, exactamente 12 sean defectuosos?


```{r }
tp <- 200
pdf <- 12
pdfi <- 0.05
probabilidad<- dbinom(pdf, tp, pdfi)
probabilidad*100
```

3. Un sitio web de citas desea medir la tasa de conversión de las personas que visitan su sitio y se suscriben a un
plan premium. Si se sabe que el 2 % de las personas que visitan el sitio se suscriben a un plan premium, ¿cuál
es la probabilidad de que de 500 visitas, exactamente 10 personas se suscriban?


```{r }
tp <- 500
pdf <- 10
pdfi <- 0.02
probabilidad<- dbinom(pdf, tp, pdfi)
probabilidad*100
```
4. Una compañía desea medir la tasa de conversión de los anuncios en línea. Si se sabe que el 20 % de las personas
que ven el anuncio hacen clic en él, ¿cuál es la probabilidad de que de 50 vistas, exactamente 12 personas
hagan clic?




```{r }
tp <- 50
pdf <- 12
pdfi <- 0.2
probabilidad<- dbinom(pdf, tp, pdfi)
probabilidad*100
```
5.Un restaurante desea medir la satisfacción de sus clientes en relación con un nuevo plato que acaba de agregar
al menú. Si se sabe que el 70 % de los clientes están satisfechos con el nuevo plato, ¿cuál es la probabilidad de
que de 100 clientes, exactamente 50 estén satisfechos?



```{r }
tp <- 100
pdf <- 50
pdfi <- 0.7
probabilidad<- dbinom(pdf, tp, pdfi)
probabilidad*100
```
6. Un fabricante de lámparas desea medir la tasa de fallas de sus productos. Si se sabe que el 3 % de las lámparas
fallan, ¿cuál es la probabilidad de que de 500 lámparas producidas, exactamente 20 fallen?




```{r }
tp <- 500
pdf <- 20
pdfi <- 0.03
probabilidad<- dbinom(pdf, tp, pdfi)
probabilidad*100
```

7. Una empresa desea medir la tasa de aprobación de sus solicitudes de crédito. Si se sabe que el 80 % de las
solicitudes son aprobadas, ¿cuál es la probabilidad de que de 200 solicitudes, exactamente 150 sean aprobadas?



```{r }
tp <- 200
pdf <- 150
pdfi <- 0.8
probabilidad<- dbinom(pdf, tp, pdfi)
probabilidad*100
```

8. Una tienda de ropa desea medir la tasa de conversión de los visitantes en compradores. Si se sabe que el 10 %
de las personas que visitan la tienda compran algo, ¿cuál es la probabilidad de que de 50 visitas, exactamente
5 personas compren?


```{r }
tp <- 50
pdf <- 5
pdfi <- 0.1
probabilidad<- dbinom(pdf, tp, pdfi)
probabilidad*100
```

9. Un fabricante de televisores desea medir la tasa de fallas de sus productos. Si se sabe que el 2 % de los televisores
fallan, ¿cuál es la probabilidad de que de 1000 televisores producidos, exactamente 15 fallen?



```{r }
tp <- 1000
pdf <- 15
pdfi <- 0.02
probabilidad<- dbinom(pdf, tp, pdfi)
probabilidad*100
```

10. Un sitio web de noticias desea medir la tasa de clics en sus artículos. Si se sabe que el 15 % de los usuarios
que visitan el sitio hacen clic en los artículos, ¿cuál es la probabilidad de que de 80 visitas, exactamente 10
usuarios hagan clic en los artículos?



```{r }
tp <- 80
pdf <- 10
pdfi <- 0.15
probabilidad<- dbinom(pdf, tp, pdfi)
probabilidad*100
```
# 2.1.1 Dedia de muestras grandes
1. Intervalo de confianza del 95 % para la altura media poblacional de los estudiantes de una universidad con una
muestra de 100 estudiantes:
Tamaño de la muestra (n): 100
Media muestral (x):$ 170 cm
Desviación estándar poblacional (σ): 8 cm (suponiendo que se conoce)
Nivel de confianza: 95 %
Valor crítico z de la distribución normal estándar para un nivel de confianza del 95 %: 1,96
Error estándar (E) : σ/p
(n) = 8/
p(100) = 0,8 cm
Intervalo de confianza: x ± zE = 170 ± 1,960,8 = (168,4, 171,6) cm
Por lo tanto, podemos afirmar con un nivel de confianza del 95 % que la altura media poblacional de los estudiantes
se encuentra dentro del intervalo (168,4, 171,6) cm.

NOTA: LOS EJERCICOS 12-22 SON LOS MISMOS QUE DEL 2-11

```{r }

```
2. Intervalo de confianza del 90 % para la temperatura media poblacional de un horno con una muestra de 50
mediciones:
Media muestral (x): 400 °C
Desviación estándar muestral (s): 10 °C

```{r }
DesvEst <- 10
n <- 50
xbarra <- 400
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha1*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha1*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")°C"); 
print(Intervalo)
```
3. Intervalo de confianza del 95 para la media de las alturas de una población de 2000 estudiantes universitarios:
Media muestral (x): 170 cm
Desviación estándar muestral (s): 10 cm


```{r }
DesvEst <- 10
n <- 2000
xbarra <- 170
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")cm"); 
print(Intervalo)
```
4. Intervalo de confianza del 99 % para la media del diámetro de un conjunto de 50 tornillos:
Media muestral (x): 3.5 mm
Desviación estándar muestral (s): 0.2 mm



```{r }
DesvEst <- 0.2
n <- 50
xbarra <- 3.5
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")mm"); 
print(Intervalo)
```

5. Intervalo de confianza del 90 para la media del peso de una población de 50000 manzanas:
Media muestral (x): 150 g
Desviación estándar muestral (s): 20 g



```{r }
DesvEst <- 20
n <- 50000
xbarra <- 150
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha1*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha1*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),"g)"); 
print(Intervalo)
```
6. Intervalo de confianza del 95 para la media del tiempo de reacción de un grupo de 100 conductores:
Media muestral (x): 0.5 s
Desviación estándar muestral (s): 0.1 s


```{r }
DesvEst <- 0.1
n <- 100
xbarra <- 0.5
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n));
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")s"); 
print(Intervalo)
```
7. Intervalo de confianza del 99 % para la media de las concentraciones de cloro en una muestra de agua:
Media muestral (x): 2.0 ppm
Desviación estándar muestral (s): 0.3 ppm


```{r }
DesvEst <- 0.3
n <- 1
xbarra <- 2.0
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")ppm"); 
print(Intervalo)
```
8. Intervalo de confianza del 90 para la media de las velocidades de una muestra de 50 coches en una autopista:
Media muestral (x): 120 km/h
Desviación estándar muestral (s): 5 km/h



```{r }
DesvEst <- 5
n <- 50
xbarra <- 120
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha1*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha1*(DesvEst/sqrt(n));

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")Km/h"); 
print(Intervalo)
```
9. Intervalo de confianza del 99 % para la media del diámetro de una muestra de 15 pernos:
Media muestral (x): 5.5 mm
Desviación estándar muestral (s): 0.3 mm


```{r }
DesvEst <- 0.3
n <- 15
xbarra <- 5.5
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n));
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n));

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")mm"); 
print(Intervalo)
```

10. Intervalo de confianza del 95 % para la media de las concentraciones de ácido sulfúrico en una muestra de 25
soluciones:
Media muestral (x): 3.2 M
Desviación estándar muestral (s): 0.4 M

```{r }
DesvEst <- 0.4
n <- 25
xbarra <- 3.2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")m"); 
print(Intervalo)
```
11. Intervalo de confianza del 90 % para la media de las resistencias eléctricas en una muestra de 20 circuitos:
Media muestral (x): 150 ohms
Desviación estándar muestral (s): 20 ohms


```{r }
DesvEst <- 20
n <- 20
xbarra <- 150
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha1*(DesvEst/sqrt(n));
mu2 <- xbarra + Zalpha1*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")ohms"); 
print(Intervalo)
```

22. Intervalo de confianza del 95 % para la media de las alturas de una muestra de 30 estudiantes de una universidad:
Media muestral (x): 170 cm Desviación estándar muestral (s): 6 cm


```{r }
DesvEst <- 0.4
n <- 25
xbarra <- 3.2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")cm"); 
print(Intervalo)
```
## ing. sistemas electronicos

1. Un fabricante de resistencias electrónicas toma una muestra aleatoria de 100 resistencias de su línea de
producción y encuentra que la media muestral de resistencia es de 500 ohms, con una desviación estándar
muestral de 10 ohms. ¿Cuál es el intervalo de confianza del 95 % para la resistencia media poblacional? Tamaño
de la muestra (n): 100 Media muestral (x): 500 ohms Desviación estándar muestral (s): 10 ohms Nivel de
confianza: 95 % Valor crítico z de la distribución normal estándar para un nivel de confianza del 95 %: 1.96
Error estándar (E): s/√n = 10/√100 = 1 ohm 
Intervalo de confianza: x ± zE = 500 ± 1,961 = (498,04, 501,96)
ohms

2. Un equipo de investigación quiere estimar la cantidad media de RAM en las computadoras portátiles producidas
por una empresa. Toman una muestra aleatoria de 200 computadoras y encuentran que la media muestral de
RAM es de 8 GB, con una desviación estándar muestral de 1 GB. ¿Cuál es el intervalo de confianza del 99 %
para la cantidad media de RAM en las computadoras portátiles producidas por la empresa?


```{r }
DesvEst <- 1
n <- 200
xbarra <- 8
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n));
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")GB"); 
Intervalo
HE<-paste0("el error estandar es:",ES," GB")
HE
```
3. En un estudio de calidad de señal en sistemas de comunicaciones, se mide la relación señal-ruido en 50 muestras
de un dispositivo. Se obtiene una media muestral de 25 dB y una desviación estándar muestral de 2 dB.
Construir un intervalo de confianza del 95 % para la verdadera relación señal-ruido media del dispositivo.



```{r }
DesvEst <-2 
n <- 50
xbarra <-25 
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")dB"); 
print(Intervalo)
HE<-paste0("el error estandar es:",ES," dB")
HE
```
4. Se desea evaluar el rendimiento de un nuevo algoritmo de compresión de datos en tiempo real. Se toman 100
mediciones del rendimiento y se obtiene una media muestral de 300 Mbps y una desviación estándar muestral
de 20 Mbps. Construir un intervalo de confianza del 99 % para la verdadera tasa de compresión media.



```{r }
DesvEst <- 20
n <- 100
xbarra <-300 
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")mbps"); 
print(Intervalo)
HE<-paste0("el error estandar es:",ES," mbps")
HE
```

5. En un estudio de fiabilidad de componentes electrónicos, se mide el tiempo de vida de 80 muestras de un
dispositivo. Se obtiene una media muestral de 5000 horas y una desviación estándar muestral de 800 horas.
Construir un intervalo de confianza del 90 % para la verdadera vida media del dispositivo.

```{r }
DesvEst <-800 
n <- 80
xbarra <- 5000
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha1*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha1*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")horas"); 
print(Intervalo)
HE<-paste0("el error estandar es:",ES," horas")
HE
```
6. Se desea evaluar el rendimiento de un sistema de detección de fallos en un sistema de control. Se toman 200
mediciones de tiempos de respuesta y se obtiene una media muestral de 10 ms y una desviación estándar
muestral de 1.5 ms. Construir un intervalo de confianza del 95 % para la verdadera velocidad de respuesta
media del sistema.


```{r }
DesvEst <- 1.5
n <- 200
xbarra <- 10
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")ms"); 
print(Intervalo)
HE<-paste0("el error estandar es:",ES," ms")
HE
```

7. En un estudio de calidad de señal en sistemas de radar, se mide la relación señal-ruido en 150 muestras de un
dispositivo. Se obtiene una media muestral de 30 dB y una desviación estándar muestral de 3 dB. Construir
un intervalo de confianza del 98 % para la verdadera relación señal-ruido media del dispositivo.


```{r }
DesvEst <-3 
n <- 150
xbarra <- 30
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
alpha <- 98; Zalpha4 <- 2.33

mu1 <- xbarra - Zalpha4*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha4*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")dB"); 
print(Intervalo)
HE<-paste0("el error estandar es:",ES," dB")
HE
```
## ing. en software
1. Se desea evaluar el rendimiento de un algoritmo de búsqueda en una base de datos. Se toman 500 mediciones
del tiempo de búsqueda y se obtiene una media muestral de 3.5 segundos y una desviación estándar muestral
de 0.5 segundos. Construir un intervalo de confianza del 95 % para la verdadera velocidad de búsqueda media.


```{r }
DesvEst <- 0.5
n <- 500
xbarra <- 3.5
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")seg"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," seg")
HE
```

2. En un estudio de usabilidad de una aplicación móvil, se mide el tiempo que tardan los usuarios en realizar una
tarea específica en la aplicación. Se toman 200 mediciones y se obtiene una media muestral de 45 segundos
y una desviación estándar muestral de 10 segundos. Construir un intervalo de confianza del 99 % para la
verdadera duración media de la tarea.


```{r }
DesvEst <- 10
n <- 200
xbarra <- 45
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")seg"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," seg")
HE
```
3. Se desea evaluar la eficacia de un nuevo algoritmo de clasificación de datos. Se toman 1000 mediciones de la
precisión de clasificación y se obtiene una media muestral del 85 % y una desviación estándar muestral del 5 %.
Construir un intervalo de confianza del 90 % para la verdadera precisión media de clasificación.



```{r }
DesvEst <- 5
n <- 1000
xbarra <- 85
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha1*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha1*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")%"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," %")
HE
```
4. En un estudio de rendimiento de un sistema de procesamiento de imágenes, se mide la velocidad de procesamiento
en milisegundos. Se toman 300 mediciones y se obtiene una media muestral de 25 ms y una desviación estándar
muestral de 3 ms. Construir un intervalo de confianza del 99 % para la verdadera velocidad media de
procesamiento.


```{r }
DesvEst <- 25
n <- 300
xbarra <-3 
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")ms"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," ms")
HE
```
5. Se desea evaluar la eficacia de un sistema de recomendación de productos en línea. Se toman 500 mediciones
de la satisfacción de los usuarios y se obtiene una media muestral de 4.2 estrellas y una desviación estándar
muestral de 0.5 estrellas. Construir un intervalo de confianza del 95 % para la verdadera satisfacción media de
los usuarios.


```{r }
DesvEst <- 0.5
n <- 500
xbarra <- 4.2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),") estrellas"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," estrellas")
HE
```
## ing. sistemas enegeticos

1. Se desea evaluar la eficacia de un sistema de energía solar para generar electricidad en una región específica.
Se toman 1000 mediciones de la producción de energía diaria y se obtiene una media muestral de 10 kWh y
una desviación estándar muestral de 2 kWh. Construir un intervalo de confianza del 95 % para la verdadera
producción media de energía diaria.


```{r }
DesvEst <- 2
n <- 1000
xbarra <- 10
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")kwh"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," kwh")
HE
```
2. En un estudio de la eficacia de un nuevo sistema de enfriamiento para una central eléctrica, se mide la
temperatura de salida del aire. Se toman 500 mediciones y se obtiene una media muestral de 20 grados Celsius
y una desviación estándar muestral de 3 grados Celsius. Construir un intervalo de confianza del 99 % para la
verdadera temperatura media de salida del aire.



```{r }
DesvEst <- 3
n <- 500
xbarra <- 20
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")°C"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," °C")
HE
```

3. Se desea evaluar la eficacia de un nuevo sistema de control de la temperatura en una planta de producción de
energía. Se toman 300 mediciones de la temperatura y se obtiene una media muestral de 50 grados Celsius y
una desviación estándar muestral de 5 grados Celsius. Construir un intervalo de confianza del 90 % para la
verdadera temperatura media.


```{r }
DesvEst <- 5
n <- 300
xbarra <- 50
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha1*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha1*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")°C"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," °C")
HE
```
4. En un estudio de la eficacia de un nuevo sistema de almacenamiento de energía, se mide la cantidad de energía
almacenada en kWh. Se toman 200 mediciones y se obtiene una media muestral de 100 kWh y una desviación
estándar muestral de 10 kWh. Construir un intervalo de confianza del 99 % para la verdadera cantidad media
de energía almacenada.



```{r }
DesvEst <- 10
n <- 200
xbarra <- 100
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")kwh"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," kwh")
HE
```
5. Se desea evaluar la eficacia de un nuevo sistema de generación de energía hidroeléctrica en una represa. Se
toman 400 mediciones de la producción de energía y se obtiene una media muestral de 50 MW y una desviación estándar muestral de 6 MW. Construir un intervalo de confianza del 95 % para la verdadera producción media
de energía


```{r }
DesvEst <- 6
n <- 400
xbarra <- 50
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")MW"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," MW")
HE
```
## Proteccion civil

1. Se desea estimar el tiempo medio que tarda una brigada de Protección Civil en llegar al lugar de una emergencia.
Se selecciona una muestra aleatoria de 100 emergencias y se obtiene un tiempo medio de 15 minutos con una
desviación estándar de 3 minutos. Se desea construir un intervalo de confianza del 95 % para la media del
tiempo de llegada de las brigadas.


```{r }
DesvEst <-3 
n <- 100
xbarra <- 15
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")min"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," min")
HE
```
2. Se quiere determinar la concentración media de un contaminante en el aire en una zona de Protección Civil.
Se toma una muestra aleatoria de 50 mediciones y se obtiene una concentración media de 0.5 ppm con una
desviación estándar de 0.1 ppm. Se desea construir un intervalo de confianza del 90 % para la media de la
concentración de contaminante en el aire.



```{r }
DesvEst <- 0.1
n <- 50
xbarra <- 0.5
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
ES<-DesvEst/sqrt(n)
mu1 <- xbarra - Zalpha1*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha1*(DesvEst/sqrt(n)); 

Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")ppm"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," ppm")
HE
```
3. Se desea determinar la media de la cantidad de material de rescate utilizado en las operaciones de Protección
Civil. Se toma una muestra aleatoria de 200 operaciones y se obtiene una media de 500 kg con una desviación
estándar de 50 kg. Se desea construir un intervalo de confianza del 99 % para la media de la cantidad de
material de rescate utilizado en las operaciones.


```{r }
DesvEst <-50 
n <- 200
xbarra <- 500
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")kg"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," kg")
HE
```
4. Se desea estimar la media del tiempo que tarda en ser restablecido el servicio eléctrico en una zona afectada
por un desastre natural. Se selecciona una muestra aleatoria de 150 casos y se obtiene un tiempo medio de
restablecimiento de 6 horas con una desviación estándar de 1.5 horas. Se desea construir un intervalo de
confianza del 95 % para la media del tiempo de restablecimiento del servicio eléctrico.



```{r }
DesvEst <- 1.5
n <- 150
xbarra <- 6
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")horas"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," horas")
HE
```
5. Se quiere estimar la media del número de llamadas de emergencia recibidas en un centro de atención de
Protección Civil durante los fines de semana. Se toma una muestra aleatoria de 300 fines de semana y se obtiene
un número medio de llamadas de 200 con una desviación estándar de 30. Se desea construir un intervalo de
confianza del 98 % para la media del número de llamadas de emergencia recibidas durante los fines de semana.


```{r }
DesvEst <- 30
n <- 300
xbarra <- 200
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
alpha <- 98; Zalpha4 <- 2.33

mu1 <- xbarra - Zalpha4*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha4*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")llamadas"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," llamadas")
HE
```
## ingenieria
1. Un fabricante de circuitos electrónicos afirma que la vida útil media de una batería de su producción es de al
menos 500 horas. Si se toma una muestra aleatoria de 100 baterías y se encuentra una vida útil media de 510
horas con una desviación estándar de 20 horas, ¿qué se puede concluir con un nivel de confianza del 95 %?


```{r }
DesvEst <- 20
n <- 100
xbarra <- 510
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")horas"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," horas")
HE
```

2. Un ingeniero quiere estimar la resistencia promedio de una muestra de 60 vigas de acero. Los datos indican
una media de 20,5 kN/m2 y una desviación estándar de 1,8 kN/m2. Si se desea tener un nivel de confianza del
99 %, ¿cuál es el intervalo de confianza para la resistencia media de las vigas?

```{r }
DesvEst <- 1.8
n <- 60
xbarra <- 20.5
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")KN/m2"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," KN/m2")
HE
```

3. Se está llevando a cabo una investigación sobre la cantidad de agua que un motor de combustión interna puede
absorber antes de que falle. Se toma una muestra de 80 motores y se encuentra una media de absorción de 80
ml con una desviación estándar de 10 ml. Si se desea estimar la cantidad media de absorción con un nivel de
confianza del 90 %, ¿cuál es el intervalo de confianza?


```{r }
DesvEst <- 10
n <- 80
xbarra <- 80
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha1*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha1*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")ml"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," ml")
HE
```

4. Un fabricante de turbinas de viento desea probar su nueva turbina en condiciones extremas de viento. Para
ello, se mide la potencia generada por la turbina en una muestra de 50 pruebas. La media de la potencia
generada es de 1200 kW con una desviación estándar de 100 kW. ¿Cuál es el intervalo de confianza del 95 %
para la potencia media de la turbina?


```{r }
DesvEst <- 100
n <- 50
xbarra <- 1200
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha2*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")kw"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," kw")
HE
```
5. Se está llevando a cabo una investigación para determinar la vida útil media de un nuevo material utilizado en
la construcción de edificios. Se toma una muestra de 200 piezas del material y se encuentra que la vida útil
media es de 20 años con una desviación estándar de 2 años. Si se desea tener un nivel de confianza del 99 %,
¿cuál es el intervalo de confianza para la vida útil media del material?



```{r }
DesvEst <- 2
n <- 200
xbarra <- 20
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57

mu1 <- xbarra - Zalpha3*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Zalpha3*(DesvEst/sqrt(n)); 
ES<-DesvEst/sqrt(n)
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")años"); 
print(Intervalo)
HE<-paste0("el error estandar es: ",ES," años")
HE
```

# 2.1.2 Muestras pequeñas 
1. Intervalo de confianza del 95 % para la altura media poblacional de los estudiantes de una universidad con una
muestra de 10 estudiantes:
Tamaño de la muestra (n): 10
Media muestral (x): 170 cm
Desviación estándar muestral (s): 8 cm

```{r }
n <- 10
xbarra <- 170
DesvEst <- 8
gl <- n-1
nc <- 0.95

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")cm"); 
print(Intervalo)
```

2. Intervalo de confianza del 90 % para el peso medio poblacional de los recién nacidos con una muestra de 15
bebés
Tamaño de la muestra (n): 15
Media muestral (x): 3.5 kg
Desviación estándar muestral (s): 0.5 kg


```{r }
n <- 15
xbarra <- 3.5
DesvEst <- 0.5
gl <- n-1
nc <- 0.90

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")kg"); 
print(Intervalo)
```

3. Intervalo de confianza del 99 para la duración media de las baterías de un dispositivo electrónico con una
muestra de 84 dispositivos:
Tamaño de la muestra (n): 84
Media muestral (x): 30 horas
Desviación estándar muestral (s): 5 horas

```{r }
n <- 84
xbarra <- 30
DesvEst <- 5
gl <- n-1
nc <- 0.99

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")horas"); 
print(Intervalo)
```

4. Intervalo de confianza del 95 % para el número medio de días de hospitalización de pacientes con una muestra
de 12 pacientes: 
Tamaño de la muestra (n): 12 
Media muestral (x): 5 días 
Desviación estándar muestral (s): 2días


```{r }
n <- 12
xbarra <- 5
DesvEst <- 2
gl <- n-1
nc <- 0.95

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")dias"); 
print(Intervalo)
```
5. Intervalo de confianza del 99 % para la media poblacional de la concentración de un compuesto en una muestra
de agua con una muestra de 7 mediciones: 
Tamaño de la muestra (n): 7 
Media muestral (x): 4 mg/L
Desviación estándar muestral (s): 1 mg/L


```{r }
n <- 7
xbarra <- 4
DesvEst <- 1
gl <- n-1
nc <- 0.99

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")mg/L"); 
print(Intervalo)
```
6. Intervalo de confianza del 95 % para la altura media de una muestra de 8 plantas: 
Tamaño de la muestra (n): 8
Media muestral (x): 12 cm 
Desviación estándar muestral (s): 2 cm 
Nivel de confianza: 95 %



```{r }
n <- 8
xbarra <- 12
DesvEst <- 2
gl <- n-1
nc <- 0.95

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")cm"); 
print(Intervalo)
```
7. Intervalo de confianza del 90 % para el tiempo medio de reacción de un grupo de 6 sujetos: 
Tamaño de la muestra (n): 6 
Media muestral (x): 0.3 segundos 
Desviación estándar muestral (s): 0.05 segundos


```{r }
n <- 6
xbarra <- 0.3
DesvEst <- 0.05
gl <- n-1
nc <- 0.90

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")cm"); 
print(Intervalo)
```
8. Intervalo de confianza del 99 % para la resistencia media de un lote de 10 piezas de acero: 
Tamaño de la muestra (n): 10 
Media muestral (x): 750 MPa 
Desviación estándar muestral (s): 25 MPa


```{r }
n <- 10
xbarra <- 750
DesvEst <- 25
gl <- n-1
nc <- 0.99

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")mPa"); 
print(Intervalo)
```
9. Intervalo de confianza del 95 % para la concentración media de un compuesto en una muestra de 12 aguas subterráneas: 
Tamaño de la muestra (n): 12 
Media muestral (x): 5 mg/L 
Desviación estándar muestral (s): 1.5 mg/L


```{r }
n <- 12
xbarra <- 5
DesvEst <- 1.5
gl <- n-1
nc <- 0.95

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")mg/L"); 
print(Intervalo)
```
10. Intervalo de confianza del 95 % para la eficiencia media de una muestra de 20 paneles solares: 
Tamaño de la muestra (n): 20 
Media muestral (x): 80 % 
Desviación estándar muestral (s): 4 %


```{r }
n <- 20
xbarra <- 80
DesvEst <- 4
gl <- n-1
nc <- 0.95

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")%"); 
print(Intervalo)
```
11. Intervalo de confianza del 90 % para la concentración media de un compuesto en una muestra de 15 suelos:
Tamaño de la muestra (n): 15 
Media muestral (x): 0.3 mg/g 
Desviación estándar muestral (s): 0.05 mg/g 
Nivel de confianza: 90 %


```{r }
n <- 15
xbarra <- 0.3
DesvEst <- 0.05
gl <- n-1
nc <- 0.90

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")mg/g"); 
print(Intervalo)
```
12. concentración media de un producto químico en una muestra de 25 aguas subterráneas: 
Tamaño de la muestra (n): 25 
Media muestral (x): 10.2 mg/L 
Desviación estándar muestral (s): 2.3 mg/L
supongamos un nivelde confianza de un 99%


```{r }
n <- 25
xbarra <- 10.2
DesvEst <- 2.3
gl <- n-1
nc <- 0.99

Talpha2 <- round(qt(nc, gl),4)
mu1 <- xbarra - Talpha2*(DesvEst/sqrt(n)); 
mu2 <- xbarra + Talpha2*(DesvEst/sqrt(n)); 
Intervalo <- paste0("mu ∈  ","(",round(mu1,2),",",round(mu2,2),")mg/L"); 
print(Intervalo)
```
# 2.2 Diferencia de 2 medias
2.2.1 Varianzas conocidas y distintas 

1. Se desea conocer si el tiempo promedio de atención en dos cajeros automáticos diferentes es significativamente
diferente. Se toman muestras aleatorias de 50 transacciones en cada cajero y se obtienen medias muestrales
de 3.5 y 4.2 minutos, respectivamente. La desviación estándar poblacional es conocida y es de 0.8 minutos.
Construir un intervalo de confianza del 95 % para la diferencia de las medias poblacionales.

```{r }
DesvEst <- 0.8
n1 <- 50
n2<-50
xbarra1 <- 3.5
xbarra2 <- 4.2
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha2

ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))


LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")minutos"); 
print(Intervalo)
```
2. Se quiere determinar si hay una diferencia significativa en la altura promedio de dos poblaciones de estudiantes
universitarios. Se toman muestras aleatorias de 100 estudiantes de cada población y se obtienen medias
muestrales de 1.72 y 1.68 metros, respectivamente. La desviación estándar poblacional es conocida y es de 0.07
metros. Construir un intervalo de confianza del 99 % para la diferencia de las medias poblacionales.


```{r }
DesvEst <- 0.07
n1 <- 100
n2<-100
xbarra1 <- 1.72
xbarra2 <- 1.68
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha3

ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))


LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")metros"); 
print(Intervalo)
```

3. Se quiere comparar la productividad de dos plantas de producción de una empresa. Se toman muestras aleatorias
de 200 trabajadores de cada planta y se obtienen medias muestrales de 150 y 140 unidades producidas por
hora, respectivamente. La desviación estándar poblacional es conocida y es de 12 unidades por hora. Construir
un intervalo de confianza del 90 % para la diferencia de las medias poblacionales.

```{r }
DesvEst <- 12
n1 <- 200
n2<-200
xbarra1 <- 150
xbarra2 <- 140
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha1
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")unidades por hora"); 
print(Intervalo)
```
4. Un fabricante de focos quiere comparar dos marcas diferentes en términos de vida útil. Toma una muestra de
100 focos de la marca A, con una vida media de 1200 horas y una desviación estándar de 200 horas, y una
muestra de 120 focos de la marca B, con una vida media de 1350 horas y una desviación estándar de 180 horas.
Construir un intervalo de confianza del 95 % para la diferencia media de vida útil entre las dos marcas.



```{r }
DesvEst <- 200
DesvEst2<-180
n1 <- 100
n2<-120
xbarra1 <- 1200
xbarra2 <- 1350
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha2
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")horas"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")horas"); 
print(Intervalo)
```
5. Un estudio quiere comparar la efectividad de dos tipos de medicamentos en el tratamiento de la hipertensión.
Se toma una muestra de 80 pacientes tratados con el medicamento A, con una presión arterial media de 130
mmHg y una desviación estándar de 10 mmHg, y una muestra de 90 pacientes tratados con el medicamento B,
con una presión arterial media de 125 mmHg y una desviación estándar de 12 mmHg. Construir un intervalo
de confianza del 99 % para la diferencia media en la presión arterial entre los dos medicamentos.


```{r }
DesvEst <- 10
DesvEst2<-12
n1 <- 80
n2<-90
xbarra1 <- 130
xbarra2 <- 125
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha3
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")mmgh"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")mmgh"); 
print(Intervalo)
```
6. Se quiere comparar dos programas de entrenamiento en términos de su efectividad en el aumento de la fuerza
muscular. Se toma una muestra de 60 atletas que han seguido el programa A, con una fuerza media de 150
kg y una desviación estándar de 20 kg, y una muestra de 70 atletas que han seguido el programa B, con una fuerza media de 155 kg y una desviación estándar de 18 kg. Construir un intervalo de confianza del 90 % para
la diferencia media en la fuerza muscular entre los dos programas.


```{r }
DesvEst <- 18
n1 <- 60
n2<-70
xbarra1 <- 150
xbarra2 <- 155
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha1
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")kg"); 
print(Intervalo)

```

7. Un estudio quiere comparar dos tipos de fertilizantes en términos de su efectividad en el crecimiento de plantas
de maíz. Se toma una muestra de 200 plantas tratadas con el fertilizante A, con una altura media de 1.8 metros
y una desviación estándar de 0.2 metros, y una muestra de 220 plantas tratadas con el fertilizante B, con una
altura media de 2.0 metros y una desviación estándar de 0.18 metros. Construir un intervalo de confianza del
95 % para la diferencia media en la altura de las plantas entre los dos fertilizantes

```{r }
DesvEst <- 0.2
DesvEst2<-0.18 
n1 <- 200
n2<-220
xbarra1 <- 1.8
xbarra2 <- 2.0
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha2
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")metros"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")metros"); 
print(Intervalo)
```
```
8. Se quiere comparar el rendimiento de dos tipos de discos duros en términos de su velocidad de lectura y
escritura. Se toma una muestra de 150 discos duros del tipo A, con una velocidad media de lectura/escritura
de 100 MB/s y una desviación estándar de 10 MB/s, y una muestra de 180 discos duros del tipo B, con
una velocidad media de lectura/escritura de 120 MB/s y una desviación estándar de 12 MB/s. Construir un
intervalo de confianza del 99 % para la diferencia media en la velocidad de lectura/escritura entre los dos tipos
de discos duros.


```{r }
DesvEst <- 10
DesvEst2<-12 
n1 <- 150
n2<-180
xbarra1 <- 100
xbarra2 <- 120
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha3
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")metros"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")metros"); 
print(Intervalo)
```

9. Un investigador desea comparar la efectividad de dos métodos de enseñanza de matemáticas en una muestra
de 60 estudiantes. El primer grupo de 30 estudiantes recibió el método A y el segundo grupo de 30 estudiantes
recibió el método B. La media y la desviación estándar de las calificaciones de los estudiantes en el grupo A
fueron de 75 y 8, respectivamente, mientras que en el grupo B fueron de 80 y 10, respectivamente. Construir
un intervalo de confianza del 95 % para la diferencia media entre las calificaciones de los dos grupos.


```{r }
DesvEst <- 8
DesvEst2<-10
n1 <- 30
n2<-30
xbarra1 <- 75
xbarra2 <- 80
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha2
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)
```

10. Se desea comparar la efectividad de dos medicamentos para reducir la presión arterial en pacientes con
hipertensión. Un grupo de 25 pacientes tomó el medicamento A y otro grupo de 30 pacientes tomó el
medicamento B. La media y la desviación estándar de la presión arterial de los pacientes en el grupo A fueron
de 140 y 10, respectivamente, mientras que en el grupo B fueron de 135 y 12, respectivamente. Construir un
intervalo de confianza del 90 % para la diferencia media entre las presiones arteriales de los dos grupos.

```{r }
DesvEst <- 10
DesvEst2<-12
n1 <- 30
n2<-30
xbarra1 <- 140
xbarra2 <- 135
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha1
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)
```
11. Se desea comparar la duración de dos tipos de baterías para un teléfono móvil. Se seleccionó una muestra
aleatoria de 50 baterías de cada tipo. La media y la desviación estándar de la duración de las baterías del tipo
A fueron de 20 horas y 3 horas, respectivamente, mientras que las del tipo B fueron de 22 horas y 4 horas,
respectivamente. Construir un intervalo de confianza del 99 % para la diferencia media entre las duraciones de
las baterías de los dos tipos.


```{r }
DesvEst <- 3
DesvEst2<-4
n1 <- 50
n2<-50
xbarra1 <- 20
xbarra2 <- 22
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha3
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")horas"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")horas"); 
print(Intervalo)
```
12. Se desea comparar el tiempo que tardan dos marcas de coches en recorrer una distancia de 100 km. Se seleccionó
una muestra aleatoria de 20 coches de cada marca. La media y la desviación estándar del tiempo que tardaron
los coches de la marca A fueron de 1.5 horas y 0.2 horas, respectivamente, mientras que los de la marca B
tardaron 1.3 horas y 0.3 horas, respectivamente. Construir un intervalo de confianza del 95 % para la diferencia
media entre los tiempos que tardan los coches de las dos marcas.


```{r }
DesvEst <- 0.2
DesvEst2<-0.3
n1 <- 100
n2<-100
xbarra1 <- 1.5
xbarra2 <- 1.3
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha2
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")horas"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")horas"); 
print(Intervalo)
```
13. Se desea comparar el rendimiento de dos tipos de fertilizantes en el crecimiento de las plantas de maíz. Se
seleccionó una muestra aleatoria de 40 plantas de cada tipo de fertilizante. La media y la desviación estándar
del crecimiento de las plantas con el fertilizante A fueron de 150 cm y 20 cm, respectivamente, mientras que
las del fertilizante B fueron de 160 cm y 18 cm, respectivamente. Construir un intervalo de confianza del 99 %
para la diferencia media entre los crecimientos de las plantas con los dos tipos de fertilizante.


```{r }
DesvEst <- 20
DesvEst2<-18
n1 <- 40
n2<-40
xbarra1 <- 150
xbarra2 <- 160
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha3
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")cm"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")cm"); 
print(Intervalo)
```
14. Se quiere comparar el tiempo promedio de entrega de dos proveedores de paquetes. Se toma una muestra de
25 paquetes del primer proveedor, con un tiempo promedio de entrega de 3.5 días y una desviación estándar
muestral de 1.2 días, y una muestra de 30 paquetes del segundo proveedor, con un tiempo promedio de entrega
de 4.2 días y una desviación estándar muestral de 1.5 días. Construya un intervalo de confianza del 95 % para
la diferencia de los tiempos promedio de entrega.


```{r }
DesvEst <- 1.2
DesvEst2<-1.5
n1 <- 25
n2<-30
xbarra1 <- 3.5
xbarra2 <- 4.2
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha2
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")dias"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")dias"); 
print(Intervalo)
```
15. Se quiere comparar el rendimiento promedio de dos grupos de estudiantes en un examen de matemáticas.
Se toma una muestra de 10 estudiantes del primer grupo, con un rendimiento promedio de 75 puntos y
una desviación estándar muestral de 8 puntos, y una muestra de 12 estudiantes del segundo grupo, con un
rendimiento promedio de 68 puntos y una desviación estándar muestral de 10 puntos. Se asume que las varianzas de ambos grupos son iguales. Construya un intervalo de confianza del 90 % para la diferencia de los
rendimientos promedio.



```{r }
DesvEst <- 8
DesvEst2<-10
n1 <- 10
n2<-12
xbarra1 <- 75
xbarra2 <- 68
xbarra<-xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc<- Zalpha1
ErrorStd <- sqrt((DesvEst/n1)+(DesvEst/n2))
LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")puntos"); 
print(Intervalo)
ErrorStd <- sqrt((DesvEst2/n1)+(DesvEst2/n2))
LimInf <- xbarra - nc*ErrorStd;
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")puntos"); 
print(Intervalo)
```
## 2.2.2. Varianzas conocidas e iguales

1. Un estudio compara los niveles de estrés entre trabajadores de dos empresas diferentes. Se toman muestras de
50 trabajadores de cada empresa. La media de estrés en la empresa 1 es de 6.2 con una desviación estándar de
1.1, mientras que la media de estrés en la empresa 2 es de 5.8 con una desviación estándar de 1.3. Construir un
intervalo de confianza al 95 % para la diferencia de medias entre ambas empresas.

```{r }
DesvEst <- 1.1
DesvEst2<-1.3
n1 <- 50
n2 <- 50
xbarra1 <- 6.2
xbarra2 <- 5.8
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha2

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)
```

2. Se quiere comparar la eficacia de dos medicamentos para tratar la hipertensión arterial. Se toma una muestra
de 100 pacientes para cada medicamento. La media de presión arterial en el grupo que recibe el medicamento 1
es de 145 mmHg con una desviación estándar de 5 mmHg, mientras que la media de presión arterial en el
grupo que recibe el medicamento 2 es de 140 mmHg con una desviación estándar de 6 mmHg. Construir un
intervalo de confianza al 99 % para la diferencia de medias entre ambos medicamentos.


```{r }
DesvEst <- 5
DesvEst2<-6
n1 <- 100
n2 <- 100
xbarra1 <- 145
xbarra2 <- 140
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha3

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")mmh"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")mmh"); 
print(Intervalo)
```

3. Se desea comparar el tiempo de respuesta de dos proveedores de internet. Se toman muestras de 30 usuarios
para cada proveedor. La media de tiempo de respuesta del proveedor 1 es de 20 milisegundos con una desviación
estándar de 3 milisegundos, mientras que la media de tiempo de respuesta del proveedor 2 es de 25 milisegundos
con una desviación estándar de 4 milisegundos. Construir un intervalo de confianza al 90 % para la diferencia
de medias entre ambos proveedores.


```{r }
DesvEst <- 3
DesvEst2<-4
n1 <- 30
n2 <- 30
xbarra1 <- 20
xbarra2 <- 25
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha1

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)

```

4. Un estudio compara los precios de productos en dos supermercados diferentes. Se toman muestras de 50
productos de cada supermercado. La media de precio en el supermercado 1 es de $5,2 con una desviación
estándar de $1,1, mientras que la media de precio en el supermercado 2 es de $4,8 con una desviación estándar
de $1,3. Construir un intervalo de confianza al 95 % para la diferencia de medias entre ambos supermercados.


```{r }
DesvEst <- 1.1
DesvEst2<-1.3
n1 <- 50
n2 <- 50
xbarra1 <- 5.2
xbarra2 <- 4.8
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha2

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")$"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")$"); 
print(Intervalo)
```

5. Se quiere comparar la efectividad de dos programas de entrenamiento para mejorar la flexibilidad. Se toman
muestras de 40 participantes para cada programa. La media de mejora en la flexibilidad en el programa 1 es
de 15 grados con una desviación estándar de 2 grados, mientras que la media de mejora en la flexibilidad en el
programa 2 es de 18 grados con una desviación estándar de 3 grados. Construir un intervalo de confianza al
99 % para la diferencia de medias entre ambos programas.

```{r }
DesvEst <- 2
DesvEst2<-3
n1 <- 40
n2 <- 40
xbarra1 <- 15
xbarra2 <- 18
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha3

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")grados"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")grados"); 
print(Intervalo)
```

6. Se quiere comparar el tiempo de reacción de dos grupos de conductores en una situación de emergencia. Se
toman muestras de 25 conductores de cada grupo. La media de tiempo de reacción del grupo 1 es de 0.8
segundos con una desviación estándar de 0.1 segundos, mientras que la media de tiempo de reacción del grupo
2 es de 0.9 segundos con una desviación estándar de 0.2 segundos. Construir un intervalo de confianza al 99 %
para la diferencia de medias entre ambos grupos.


```{r }
DesvEst <- 0.1
DesvEst2<-0.2
n1 <- 25
n2 <- 25
xbarra1 <- 0.8
xbarra2 <- 0.9
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha3

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")seg"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")seg"); 
print(Intervalo)
```
7. Se desea comparar la eficacia de dos métodos de enseñanza de matemáticas en alumnos de tercer grado de
primaria. Se toma una muestra de 50 alumnos para cada método. La media del puntaje en el examen del
método 1 es de 70 puntos con una desviación estándar de 5 puntos, mientras que la media del puntaje en el
examen del método 2 es de 75 puntos con una desviación estándar de 6 puntos. Construir un intervalo de
confianza al 95 % para la diferencia de medias entre ambos métodos.


```{r }
DesvEst <- 5
DesvEst2<-6
n1 <- 50
n2 <- 50
xbarra1 <- 70
xbarra2 <- 75
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha2

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")puntos"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")puntos"); 
print(Intervalo)
```
8. Un estudio compara la tasa de mortalidad en dos hospitales. Se toman muestras de 100 pacientes de cada
hospital. La tasa de mortalidad en el hospital 1 es del 5 % con una desviación estándar del 1 %, mientras que la tasa de mortalidad en el hospital 2 es del 8 % con una desviación estándar del 2 %. Construir un intervalo de
confianza al 90 % para la diferencia de proporciones entre ambos hospitales.


```{r }
DesvEst <- 1
DesvEst2<-2
n1 <- 100
n2 <- 100
xbarra1 <- 5
xbarra2 <- 8
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha1

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")%"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")%"); 
print(Intervalo)
```
9. Se quiere comparar el rendimiento académico entre estudiantes de dos escuelas diferentes. Se toman muestras
de 30 estudiantes de cada escuela. La media de calificaciones en la escuela 1 es de 8.5 con una desviación
estándar de 1.2, mientras que la media de calificaciones en la escuela 2 es de 7.8 con una desviación estándar
de 1.5. Construir un intervalo de confianza al 99 % para la diferencia de medias entre ambas escuelas.



```{r }
DesvEst <- 1.2
DesvEst2<-1.5
n1 <- 30
n2 <- 30
xbarra1 <- 8.5
xbarra2 <- 7.8
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha3

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)
```

10. Se toman dos muestras de tamaño 100 cada una. La primera muestra tiene una media de 25 y una desviación
estándar de 5, mientras que la segunda muestra tiene una media de 30 y una desviación estándar de 6. Construir
un intervalo de confianza al 95 % para la diferencia de medias entre ambas poblaciones.

```{r }
DesvEst <- 5
DesvEst2<-6
n1 <- 100
n2 <- 100
xbarra1 <- 25
xbarra2 <- 30
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha2

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)
```

11. Se quiere comparar la eficacia de dos tratamientos para reducir el dolor de cabeza. Se toma una muestra de
150 pacientes para cada tratamiento. La media del grupo 1 es de 6.5 y la del grupo 2 es de 5.8. La desviación
estándar en ambos casos es de 1.2. Construir un intervalo de confianza al 99 % para la diferencia de medias
entre ambos tratamientos.


```{r }
DesvEst <- 1.2
n1 <- 150
n2 <- 150
xbarra1 <- 6.5
xbarra2 <- 5.8
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha3

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)
```
12. Se desea comparar la velocidad de procesamiento de dos computadoras diferentes. Se toman muestras de
50 usuarios para cada computadora. La media de velocidad de la computadora 1 es de 4.2 GHz con una
desviación estándar de 0.5 GHz, mientras que la media de velocidad de la computadora 2 es de 4.5 GHz con
una desviación estándar de 0.6 GHz. Construir un intervalo de confianza al 90 % para la diferencia de medias
entre ambas computadoras.



```{r }
DesvEst <- 0.5
DesvEst2<-0.6
n1 <- 50
n2 <- 50
xbarra1 <- 4.2
xbarra2 <- 4.5
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha1

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")GHZ"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")GHZ"); 
print(Intervalo)
```

13. Se quiere comparar el rendimiento académico de estudiantes de dos escuelas diferentes. Se toman muestras
de 80 estudiantes para cada escuela. La media de calificaciones en la escuela 1 es de 7.8 con una desviación
estándar de 1.2, mientras que la media de calificaciones en la escuela 2 es de 8.2 con una desviación estándar
de 1.3. Construir un intervalo de confianza al 95 % para la diferencia de medias entre ambas escuelas.


```{r }
DesvEst <- 1.2
DesvEst2<-1.3
n1 <- 80
n2 <- 80
xbarra1 <- 7.8
xbarra2 <- 8.2
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha2

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); 
print(Intervalo)
```
14. Se desea comparar la eficacia de dos programas de entrenamiento para corredores. Se toman muestras de
200 corredores para cada programa. La media de tiempo de carrera del programa 1 es de 30 minutos con
una desviación estándar de 2.5 minutos, mientras que la media de tiempo de carrera del programa 2 es de
28 minutos con una desviación estándar de 2.7 minutos. Construir un intervalo de confianza al 99 % para la
diferencia de medias entre ambos programas.


```{r }
DesvEst <- 2.5
DesvEst2<-2.7
n1 <- 200
n2 <- 200
xbarra1 <- 30
xbarra2 <- 28
xbarra <- xbarra1-xbarra2
alpha <- 90; Zalpha1 <- 1.64
alpha <- 95; Zalpha2 <- 1.96
alpha <- 99; Zalpha3 <- 2.57
nc <- Zalpha3

ErrorStd <- DesvEst*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")MIN"); 
print(Intervalo)

ErrorStd <- DesvEst2*sqrt((n1+n2)/(n1*n2))
ErrorStd <- DesvEst2*sqrt((1/n1)+(1/n2))

LimInf <- xbarra - nc*ErrorStd; 
LimSup <- xbarra + nc*ErrorStd; 

Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")MIN"); 
print(Intervalo)
```
## 2.2.3. Varianzas desconocidas e iguales


1. Se quiere comparar la duración de dos tipos de baterías en un mismo dispositivo electrónico. Se toman muestras
de 10 baterías de cada tipo. La duración media de las baterías tipo A es de 25 horas con una desviación
estándar de 3 horas, mientras que la duración media de las baterías tipo B es de 30 horas con una desviación estándar de 4 horas. Construir un intervalo de confianza al 95 % para la diferencia de medias entre ambas
baterías.


```{r }
n1 <- 10
n2 <- 10
xbarra1 <- 25
xbarra2 <- 30
S1 <- 3
S2 <- 4
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")horas"); Intervalo

```
2. Un estudio compara el efecto de dos tratamientos para reducir el dolor en pacientes con artritis. Se toman
muestras de 15 pacientes para cada tratamiento. La media de reducción del dolor en el tratamiento 1 es de 2.5
con una desviación estándar de 0.8, mientras que la media de reducción del dolor en el tratamiento 2 es de 3
con una desviación estándar de 0.9. Construir un intervalo de confianza al 99 % para la diferencia de medias
entre ambos tratamientos.



```{r }
n1 <- 15
n2 <- 15
xbarra1 <- 2.5
xbarra2 <- 3
S1 <- 0.8
S2 <- 0.9
xbarra <- xbarra1-xbarra2
nc <- 0.99
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); Intervalo
```
3. Se quiere comparar la calidad de dos marcas de café. Se toman muestras de 8 tazas de cada marca. La media
del puntaje en sabor del café de la marca A es de 8 con una desviación estándar de 1.2, mientras que la media
del puntaje en sabor del café de la marca B es de 7 con una desviación estándar de 1.1. Construir un intervalo
de confianza al 90 % para la diferencia de medias entre ambas marcas.


```{r }
n1 <- 8
n2 <- 8
xbarra1 <- 8
xbarra2 <- 7
S1 <- 1.2
S2 <- 1.1
xbarra <- xbarra1-xbarra2
nc <- 0.90
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")puntos"); Intervalo
```
4. Un estudio compara el efecto de dos programas de entrenamiento en la fuerza muscular. Se toman muestras
de 12 atletas para cada programa. La media de ganancia de fuerza en el programa 1 es de 10 kg con una
desviación estándar de 2 kg, mientras que la media de ganancia de fuerza en el programa 2 es de 12 kg con
una desviación estándar de 3 kg. Construir un intervalo de confianza al 95 % para la diferencia de medias entre
ambos programas.


```{r }
n1 <- 12
n2 <- 12
xbarra1 <- 10
xbarra2 <- 12
S1 <- 2
S2 <- 3
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")kg"); Intervalo
```
5. Se quiere comparar la eficacia de dos métodos de enseñanza de idiomas en estudiantes de secundaria. Se toman
muestras de 5 estudiantes para cada método. La media del puntaje en el examen del método 1 es de 85 puntos
con una desviación estándar de 6 puntos, mientras que la media del puntaje en el examen del método 2 es
de 80 puntos con una desviación estándar de 7 puntos. Construir un intervalo de confianza al 99 % para la
diferencia de medias entre ambos métodos.


```{r }
n1 <- 5
n2 <- 5
xbarra1 <- 85
xbarra2 <- 80
S1 <- 6
S2 <- 7
xbarra <- xbarra1-xbarra2
nc <- 0.99
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")puntos"); Intervalo
```
6. Un estudio compara la efectividad de dos medicamentos para reducir la presión arterial en pacientes con
hipertensión. Se toman muestras de 6 pacientes para cada medicamento. La media de la presión arterial en el
medicamento 1 es de 140 mmHg con una desviación estándar de 5 mmHg, mientras que la media de la presión
arterial en el medicamento 2 es de 135 mmHg con una desviación estándar de 4 mmHg. Construir un intervalo
de confianza al 95 % para la diferencia de medias entre ambos medicamentos.


```{r }
n1 <- 6
n2 <- 6
xbarra1 <- 140
xbarra2 <- 135
S1 <- 5
S2 <- 4
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")mmHg"); Intervalo
```

7. Se quiere comparar el rendimiento de dos marcas de neumáticos en condiciones de lluvia. Se toman muestras
de 10 neumáticos de cada marca. La distancia de frenado media de los neumáticos de la marca A es de 25
metros con una desviación estándar de 2 metros, mientras que la distancia de frenado media de los neumáticos
de la marca B es de 22 metros con una desviación estándar de 3 metros. Construir un intervalo de confianza al
90 % para la diferencia de medias entre ambas marcas.

```{r }
n1 <- 10
n2 <- 10
xbarra1 <- 25
xbarra2 <- 22
S1 <- 2
S2 <- 3
xbarra <- xbarra1-xbarra2
nc <- 0.90
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")metros"); Intervalo
```
8. Un estudio compara la calidad de dos tipos de lentes de contacto. Se toman muestras de 8 lentes de contacto
de cada tipo. La media de la duración de los lentes de contacto del tipo 1 es de 14 horas con una desviación
estándar de 1.2 horas, mientras que la media de la duración de los lentes de contacto del tipo 2 es de 15 horas
con una desviación estándar de 1.4 horas. Construir un intervalo de confianza al 95 % para la diferencia de
medias entre ambos tipos.

```{r }
n1 <- 8
n2 <- 8
xbarra1 <- 14
xbarra2 <- 15
S1 <- 1.2
S2 <- 1.4
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")horas"); Intervalo
```

9. Se quiere comparar el rendimiento de dos tipos de lápices en la escritura en papel rugoso. Se toman muestras
de 12 lápices de cada tipo. La media de la escritura del lápiz tipo A es de 5.5 con una desviación estándar de
0.8, mientras que la media de la escritura del lápiz tipo B es de 6 con una desviación estándar de 1.1. Construir
un intervalo de confianza al 99 % para la diferencia de medias entre ambos tipos.


```{r }
n1 <- 12
n2 <- 12
xbarra1 <- 5.5
xbarra2 <- 6
S1 <- 0.8
S2 <- 1.1
xbarra <- xbarra1-xbarra2
nc <- 0.99
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")"); Intervalo
```
10. Un estudio compara el efecto de dos tipos de fertilizantes en el crecimiento de plantas. Se toman muestras de
15 plantas para cada tipo de fertilizante. La media de la altura de las plantas con el fertilizante 1 es de 25 cm
con una desviación estándar de 3 cm, mientras que la media de la altura de las plantas con el fertilizante 2 es
de 28 cm con una desviación estándar de 4 cm. Construir un intervalo de confianza al 90 % para la diferencia
de medias entre ambos fertilizantes.


```{r }
n1 <- 15
n2 <- 15
xbarra1 <- 25
xbarra2 <- 28
S1 <- 3
S2 <- 4
xbarra <- xbarra1-xbarra2
nc <- 0.90
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")cm"); Intervalo
```
11. Se quiere comparar la efectividad de dos programas de entrenamiento en fuerza para atletas. Se toman muestras
aleatorias de 8 atletas para cada programa y se obtienen los siguientes resultados en aumento de peso levantado en kg: programa A (44, 37, 50, 41, 48, 36, 45, 38) y programa B (42, 39, 44, 40, 37, 47, 45, 41). Construir
un intervalo de confianza al 95 % para la diferencia de medias de aumento de peso levantado entre ambos
programas.



```{r }
n1 <- 8
n2 <- 8
a=44;b=37;c=50;d=41;e=48;f=36;g=45;h=38;
xbarra1 <- ((a+b+c+d+e+f+g+h)/n1)
a1=42;b1=39;c1=44;d1=40;e1=37;f1=47;g1=45;h1=41;
xbarra2 <- ((a1+b1+c1+d1+e1+f1+g1+h1)/n2)
xbarra <- xbarra1-xbarra2
dv1<-c(44, 37, 50, 41, 48, 36, 45, 38)
S1<-sd (dv1)
dv2<-c(42, 39, 44, 40, 37, 47, 45, 41)
S2<-sd (dv2)
nc <- 0.95
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")kg"); Intervalo
```
12. Un estudio comparativo quiere analizar si hay una diferencia en la cantidad de calorías quemadas durante
dos tipos de entrenamiento cardiovascular. Se toman muestras aleatorias de 10 personas para cada tipo de
entrenamiento y se obtienen los siguientes resultados en calorías quemadas: tipo A (124, 134, 137, 120, 129,
132, 136, 131, 130, 128) y tipo B (136, 125, 128, 122, 130, 132, 138, 129, 131, 133). Construir un intervalo de
confianza al 90 % para la diferencia de medias de calorías quemadas entre ambos tipos de entrenamiento.


```{r }
n1 <- 10
n2 <- 10
a=124;b=134;c=137;d=120;e=129;f=132;g=136;h=131;i=130;j=128;
xbarra1 <- ((a+b+c+d+e+f+g+h+i+j)/n1)
a1=136;b1=125;c1=128;d1=122;e1=130;f1=132;g1=138;h1=129;i1=131;j1=133;
xbarra2 <- ((a1+b1+c1+d1+e1+f1+g1+h1+i1+j1)/n2)
xbarra <- xbarra1-xbarra2
dv1<-c(124, 134, 137, 120, 129,
132, 136, 131, 130, 128)
S1<-sd (dv1)
dv2<-c(136, 125, 128, 122, 130, 132, 138, 129, 131, 133)
S2<-sd (dv2)
nc <- 0.90
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")calorias"); Intervalo
```
13. Se quiere comparar el tiempo promedio de entrega de dos compañías de paquetería. Se toman muestras
aleatorias de 12 entregas para cada compañía y se obtienen los siguientes resultados en días: compañía A (4, 5,
6, 3, 4, 5, 6, 3, 4, 5, 6, 3) y compañía B (5, 6, 4, 5, 6, 3, 4, 6, 5, 4, 6, 5). Construir un intervalo de confianza al
99 % para la diferencia de medias de tiempo de entrega entre ambas compañías.


```{r }
n1 <- 12
n2 <- 12
a=4;b=5;c=6;d=3;e=4;f=5;g=6;h=3;i=4;j=5;k=6;l=3;
xbarra1 <- ((a+b+c+d+e+f+g+h+i+j+k+l)/n1)
a1=5;b1=6;c1=4;d1=5;e1=6;f1=3;g1=4;h1=6;i1=5;j1=4;k1=6;l1=5
xbarra2 <- ((a1+b1+c1+d1+e1+f1+g1+h1+i1+j1+k1+l1)/n2)
xbarra <- xbarra1-xbarra2
dv1<-c(4, 5,
6, 3, 4, 5, 6, 3, 4, 5, 6, 3)
S1<-sd (dv1)
dv2<-c(5, 6, 4, 5, 6, 3, 4, 6, 5, 4, 6, 5)
S2<-sd (dv2)
nc <- 0.99
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")dias"); Intervalo
```

14. Se quiere comparar la efectividad de dos tratamientos para reducir los niveles de colesterol en la sangre. Se
toman muestras aleatorias de 15 pacientes para cada tratamiento y se obtienen los siguientes resultados en
mg/dL: tratamiento A (182, 172, 193, 198, 168, 178, 186, 192, 182, 175, 168, 177, 183, 188, 178) y tratamiento
B (188, 198, 183, 192, 202, 194, 187, 182, 189, 193, 196, 185, 182, 197, 190). Construir un intervalo de confianza
al 95 % para la diferencia de medias de niveles de colesterol entre ambos tratamientos.


```{r }
n1 <- 15
n2 <- 15
a=182;b=172;c=193;d=198;e=168;f=178;g=186;h=192;i=182;j=175;k=168;l=177;
m=183;n=188;ñ=178
xbarra1 <- ((a+b+c+d+e+f+g+h+i+j+k+l+m+n+ñ)/n1)
a1=188;b1=198;c1=183;d1=192;e1=202;f1=194;g1=187;h1=182;i1=189;j1=193;
k1=196;l1=185;m1=182;n1=197;ñ1=190;
xbarra2 <- ((a1+b1+c1+d1+e1+f1+g1+h1+i1+j1+k1+l1+m1+n1+ñ1)/n2)
xbarra <- xbarra1-xbarra2
dv1<-c(182, 172, 193, 198, 168, 178, 186, 192, 182, 175, 168, 177, 183, 188, 178)
S1<-sd (dv1)
dv2<-c(188, 198, 183, 192, 202, 194, 187, 182, 189, 193, 196, 185, 182, 197, 190)
S2<-sd (dv2)
nc <- 0.95
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")dias"); Intervalo
```

15. Se quiere comparar la productividad en la línea de producción de dos turnos de trabajo. Se toman muestras
aleatorias de 7 horas para cada turno y se obtienen los siguientes resultados en unidades producidas: turno A
(57, 60, 59, 62, 58, 56, 61) y turno B (63, 61, 64, 60, 62, 61, 63). Construir un intervalo de confianza al 95 %
para la diferencia de medias de productividad entre ambos.


```{r }
n1 <- 7
n2 <- 7
a=57;b=60;c=59;d=62;e=58;f=56;g=61;
xbarra1 <- ((a+b+c+d+e+f+g)/n1)
a1=63;b1=61;c1=64;d1=60;e1=62;f1=61;g1=63;
xbarra2 <- ((a1+b1+c1+d1+e1+f1+g1)/n2)
xbarra <- xbarra1-xbarra2
dv1<-c(57, 60, 59, 62, 58, 56, 61)
S1<-sd (dv1)
dv2<-c(63, 61, 64, 60, 62, 61, 63)
S2<-sd (dv2)
nc <- 0.95
Sd <- sqrt(((n1-1)*S1^2+(n2-1)*S2^2)/(n1+n2-2))
gl = n1+n2-2
Talpha2 <- round(qt(nc, gl),4)
LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",round(LimInf,2),",",
                    round(LimSup,2),")dias"); Intervalo
```
# 2.2.4. Varianzas desconocidas y distintas
1. Se quiere comparar la efectividad de dos medicamentos para el dolor de cabeza. Se toma una muestra de
10 pacientes para el medicamento A y otra muestra de 12 pacientes para el medicamento B. Los resultados
muestran que la media del medicamento A es de 6.5 y la media del medicamento B es de 5.8. Las desviaciones
estándar son de 0.7 para el medicamento A y 0.9 para el medicamento B. Construya un intervalo de confianza
del 95 % para la diferencia de medias.


```{r }
n1 <- 10
n2 <- 12
xbarra1 <- 6.5
xbarra2 <- 5.8
S1 <- 0.7
S2 <- 0.9
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")"); 
print(Intervalo)
```
2. Un gerente de recursos humanos quiere comparar los salarios promedio de dos departamentos de su empresa. Se
toma una muestra de 8 empleados del departamento A con una media de salario de $25,000 y una desviación
estándar de $1,500. Se toma otra muestra de 12 empleados del departamento B con una media de salario de $28,000 y una desviación estándar de $2,000. Calcule un intervalo de confianza del 90 % para la diferencia de
salarios promedio.



```{r }
n1 <- 8
n2 <- 12
xbarra1 <- 25000
xbarra2 <- 28000
S1 <- 1500
S2 <- 2000
xbarra <- xbarra1-xbarra2
nc <- 0.90
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")$"); 
print(Intervalo)
```
3. Se quiere comparar la eficacia de dos tipos de fertilizantes en el crecimiento de plantas. Se toman dos muestras
de 15 plantas cada una y se miden las alturas. La media de las alturas para el fertilizante A es de 16 cm con
una desviación estándar de 2 cm, mientras que la media de las alturas para el fertilizante B es de 18 cm con
una desviación estándar de 3 cm. Construya un intervalo de confianza del 99 % para la diferencia de medias.


```{r }
n1 <- 15
n2 <- 12
xbarra1 <- 16
xbarra2 <- 18
S1 <- 2
S2 <- 3
xbarra <- xbarra1-xbarra2
nc <- 0.99
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")cm"); 
print(Intervalo)
```
4. Un equipo de investigadores quiere comparar la duración media de dos tipos de baterías para dispositivos
móviles. Se toma una muestra de 10 baterías del tipo A con una duración media de 15 horas y una desviación
estándar de 1.2 horas. Se toma otra muestra de 12 baterías del tipo B con una duración media de 13 horas
y una desviación estándar de 1.5 horas. Construya un intervalo de confianza del 95 % para la diferencia de
duraciones medias.



```{r }
n1 <- 10
n2 <- 12
xbarra1 <- 15
xbarra2 <- 13
S1 <- 1.2
S2 <-1.5
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")horas"); 
print(Intervalo)
```

5. Un estudio quiere comparar el rendimiento promedio de dos tipos de semillas de tomate. Se toman dos muestras
de 20 plantas cada una y se miden las producciones de tomate. La media de producción para las semillas del
tipo A es de 30 kg con una desviación estándar de 4 kg, mientras que la media de producción para las semillas
del tipo B es de 35 kg con una desviación estándar de 6 kg. Construya un intervalo de confianza del 99 % para
la diferencia de medias.

```{r }
n1 <- 20
n2 <- 20
xbarra1 <- 30
xbarra2 <- 35
S1 <- 4
S2 <-6
xbarra <- xbarra1-xbarra2
nc <- 0.99
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")kg"); 
print(Intervalo)
```
6. Se quiere comparar la eficacia de dos métodos de enseñanza de matemáticas en dos grupos de estudiantes, con
tamaños de muestra de n1 = 20 y n2 = 25. Se obtuvieron las siguientes estadísticas muestrales: d1 = 70, s1 = 8
y d2 = 74, s2 = 10. Construir un intervalo de confianza del 95 % para la diferencia de las medias poblacionales.


```{r }
n1 <- 20
n2 <- 25
xbarra1 <- 70
xbarra2 <- 74
S1 <- 8
S2 <-10
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")"); 
print(Intervalo)
```
7. Se desea comparar los tiempos de reacción de dos tipos de conductores al conducir en condiciones de lluvia,
con tamaños de muestra de n1 = 15 y n2 = 20. Los tiempos de reacción en segundos se registraron y se
obtuvieron las siguientes estadísticas muestrales: d1 = 2,5, s1 = 0,6 y d2 = 2,1, s2 = 0,5. Construir un intervalo
de confianza del 99 % para la diferencia de las medias poblacionales.


```{r }
n1 <- 15
n2 <- 20
xbarra1 <- 2.5
xbarra2 <- 2.1
S1 <- 0.6
S2 <-0.5
xbarra <- xbarra1-xbarra2
nc <- 0.99
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")segundos"); 
print(Intervalo)
```
8. Se realizó un estudio para comparar el desempeño de dos grupos de trabajadores en una empresa en términos
de productividad. Se obtuvieron muestras de tamaños n1 = 18 y n2 = 25, y se calcularon las siguientes
estadísticas muestrales: d1 = 120, s1 = 6 y d2 = 125, s2 = 8,5. Construir un intervalo de confianza del 90 %
para la diferencia de las medias poblacionales.



```{r }
n1 <- 18
n2 <- 25
xbarra1 <- 120
xbarra2 <- 125
S1 <- 1
S2 <-8.5
xbarra <- xbarra1-xbarra2
nc <- 0.90
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")produccion"); 
print(Intervalo)
```
9. Se quiere comparar la efectividad de dos tratamientos médicos para reducir los niveles de colesterol en pacientes
con enfermedad cardiovascular. Se seleccionaron muestras de tamaños n1 = 12 y n2 = 15, y se obtuvieron las
siguientes estadísticas muestrales: d1 = 15, s1 = 3,5 y d2 = 12, s2 = 4. Construir un intervalo de confianza del
95 % para la diferencia de las medias poblacionales.


```{r }
n1 <- 12
n2 <- 15
xbarra1 <- 15
xbarra2 <- 12
S1 <- 3.5
S2 <-4
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")"); 
print(Intervalo)
```

10. Se desea comparar la precisión de dos marcas de básculas en la medición de pesos de objetos pequeños. Se
tomaron muestras de tamaños n1 = 10 y n2 = 15, y se obtuvieron las siguientes estadísticas muestrales:
d1 = 50, s1 = 1,5 y d2 = 51, s2 = 2. Construir un intervalo de confianza del 99 % para la diferencia de las
medias poblacionales.

```{r }
n1 <- 10
n2 <- 15
xbarra1 <- 50
xbarra2 <- 51
S1 <- 1.5
S2 <-2
xbarra <- xbarra1-xbarra2
nc <- 0.99
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")"); 
print(Intervalo)
```

11. Se realizó un experimento para evaluar la efectividad de dos tipos de fertilizantes en la producción de maíz.
Se tomaron muestras de tamaños n1 = 20 y n2 = 25, y se obtuvieron las siguientes estadísticas muestrales:
d1 = 150, s1 = 12 y d2 = 145, s2 = 10. Construir un intervalo de confianza del 95 % para la diferencia de las
medias poblacionales.

```{r }
n1 <- 20
n2 <- 25
xbarra1 <- 150
xbarra2 <- 145
S1 <- 12
S2 <-10
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")"); 
print(Intervalo)
```
12. Se quiere comparar la cantidad promedio de horas de sueño entre dos grupos de estudiantes universitarios: uno
que consume bebidas energéticas y otro que no. Se toma una muestra de 10 estudiantes de cada grupo y se
obtiene que el grupo que consume bebidas energéticas duerme en promedio 6.2 horas y tiene una desviación
estándar de 1.2 horas, mientras que el grupo que no consume bebidas energéticas duerme en promedio 7.5
horas y tiene una desviación estándar de 1.8 horas. ¿A un nivel de confianza del 95 %, se puede concluir que
hay una diferencia significativa en la cantidad promedio de horas de sueño entre los dos grupos?



```{r }
n1 <- 10
n2 <- 10
xbarra1 <- 6.2
xbarra2 <- 7.5
S1 <- 1.2
S2 <-1.8
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")horas"); 
print(Intervalo)
```
13. Se quiere comparar el tiempo promedio de reacción entre dos grupos de conductores: uno que usa un nuevo sistema de alerta de colisión y otro que no. Se toma una muestra de 15 conductores de cada grupo y se obtiene
que el grupo que usa el nuevo sistema tiene un tiempo promedio de reacción de 0.8 segundos y una desviación
estándar de 0.2 segundos, mientras que el grupo que no usa el sistema tiene un tiempo promedio de reacción de
0.9 segundos y una desviación estándar de 0.3 segundos. ¿A un nivel de confianza del 99 %, se puede concluir
que el nuevo sistema de alerta de colisión tiene un efecto significativo en el tiempo promedio de reacción de los
conductores?



```{r }
n1 <- 15
n2 <- 15
xbarra1 <- 0.8
xbarra2 <- 0.9
S1 <- 0.2
S2 <-0.3
xbarra <- xbarra1-xbarra2
nc <- 0.99
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")segundos"); 
print(Intervalo)
```
```
14. Un estudio compara la efectividad de dos métodos de enseñanza de matemáticas en dos grupos de estudiantes,
con tamaños de muestra de 12 y 15 respectivamente. La media y desviación estándar de los resultados obtenidos
en cada grupo son: grupo 1 (media= 75, s = 10) y grupo 2 (media= 80, s = 12). Construir un intervalo de
confianza del 95 % para la diferencia de las medias poblacionales.



```{r }
n1 <- 12
n2 <- 15
xbarra1 <- 75
xbarra2 <- 80
S1 <- 10
S2 <-12
xbarra <- xbarra1-xbarra2
nc <- 0.95
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")"); 
print(Intervalo)
```
15. Un psicólogo quiere comparar la autoestima de un grupo de pacientes con ansiedad con un grupo de pacientes
sin ansiedad. Para ello, toma una muestra de 10 pacientes con ansiedad y otra muestra de 8 pacientes sin
ansiedad. La media y desviación estándar de la autoestima de los pacientes con ansiedad son 25 y 3, mientras
que para los pacientes sin ansiedad son 30 y 4. Construir un intervalo de confianza del 99 % para la diferencia
de las medias poblacionales.



```{r }
n1 <- 10
n2 <- 8
xbarra1 <- 25
xbarra2 <- 30
S1 <- 3
S2 <-4
xbarra <- xbarra1-xbarra2
nc <- 0.99
Sd <- sqrt(((S1^2)/n1)+((S2^2)/n2))

numerador <- (((S1^2)/n1)+((S2^2)/n2))^2
denominador <- ((((S1^2)/n1)^2)/(n1-1))+((((S2^2)/n2)^2)/(n2-1))
gl = ceiling(numerador/denominador)
Talpha2 <- round(qt(nc, gl),4)

LimInf <- xbarra - Talpha2*(Sd); 
LimSup <- xbarra + Talpha2*(Sd); 
Intervalo <- paste0("mu ∈  ","(",
                    round(LimInf,2),
                    ",",
                    round(LimSup,2),
                    ")"); 
print(Intervalo)
```




```{r }

```




```{r }

```




```{r }

```




```{r }

```




```{r }

```